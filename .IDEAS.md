# Idea Generation Run (2026-02-16 13:19:46)

## pydont-disrespect-the-zen-of-python
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/02.pydonts/pydont-disrespect-the-zen-of-python/item.md`
- Length: `medium` (1487 words)
- Ideas:
1. Tip idea: Show a before/after refactor where a manual accumulator loop is replaced with `sum()` (or `any()`/`all()`), emphasizing that semantic built-ins are often *more explicit* than low-level loops.
2. Tip idea: Demonstrate “refuse the temptation to guess” by replacing silent fallback logic (like returning `None` on bad input) with a clear `raise ValueError(...)` that fails fast and explains what went wrong.
3. Tip idea: Create a quick “Zen-driven cleanup” checklist for PRs: reduce nesting by early returns, rename vague variables, and split dense one-liners so the final code reads naturally left-to-right.

## pydont-manifesto
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/02.pydonts/pydont-manifesto/item.md`
- Length: `short` (422 words)
- Ideas:
1. Build a 'bad vs good' habit: whenever you write a loop-heavy solution, immediately rewrite it once using a Pythonic tool (e.g., comprehension, `any`, `sum`, `zip`) and keep the version that is shorter and clearer.

## 001.ceiling-division-in-python
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/001.ceiling-division-in-python/item.md`
- Length: `short` (449 words)
- Ideas:
1. Use `-(-a // b)` for integer ceiling division without importing `math`: it gives `ceil(a / b)` correctly for positive and negative `a`/`b` (e.g., chunk count with `pages = -(-total_items // page_size)`).

## 003.icpo-rule-for-attribute-lookup
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/003.icpo-rule-for-attribute-lookup/item.md`
- Length: `short` (532 words)
- Ideas:
1. Debug weird method behavior by checking ICPO lookup directly: compare `obj.__dict__` (instance attrs) with `type(obj).__dict__` (class attrs), then `del obj.attr` to confirm whether an instance attribute is shadowing a class method.

## 004.c3-linearisation
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/004.c3-linearisation/item.md`
- Length: `short` (501 words)
- Ideas:
1. Debug multiple inheritance surprises by printing `YourClass.__mro__` (or `YourClass.mro()`) to see the exact C3 lookup order Python will follow for methods like `__repr__` and other attributes.

## 005.string-unpacking
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/005.string-unpacking/item.md`
- Length: `short` (288 words)
- Ideas:
1. Use direct string unpacking for fixed-width text: `year, month, day = "202" + "406"` style is fragile, but `a, b = "Hi"` or `first, *middle, last = text` cleanly extracts characters and raises `ValueError` if the length is not what you expect.

## 006.file-unpacking
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/006.file-unpacking/item.md`
- Length: `short` (263 words)
- Ideas:
1. Use iterable unpacking on a file object to split a header from remaining lines in one step: `with open('my_data.csv') as f: header, *rows = (line.rstrip('\n') for line in f)`; this is a quick REPL trick for small files before switching to `csv` for real parsing.

## 007.nextafter
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/007.nextafter/item.md`
- Length: `short` (681 words)
- Ideas:
1. Use `math.nextafter` to generate boundary float test cases around zero and one (e.g., `math.nextafter(0.0, 1.0)`, `math.nextafter(0.0, -1.0)`, `math.nextafter(1.0, math.inf)`) so you can catch precision-sensitive bugs in comparisons and sign/rounding logic.

## 008.two-pass-join
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/008.two-pass-join/item.md`
- Length: `short` (611 words)
- Ideas:
1. When building a string with `''.join(...)`, prefer a list comprehension over a generator expression in hot paths (e.g., `''.join([f(x) for x in data])`), because `str.join` makes two passes and can run faster with a prebuilt list.

## 009.vars
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/009.vars/item.md`
- Length: `short` (377 words)
- Ideas:
1. Use `vars(obj)` to quickly inspect an instance’s current attribute values while debugging, e.g. `print(vars(user))`, and prefer it over `dir(obj)` when you need names plus values instead of just attribute names.

## Run summary
- Selected: 10
- Succeeded: 10
- Failed: 0
- Retries used: 0


# Idea Generation Run (2026-02-16 14:17:13)

## 010.generator-return
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/010.generator-return/item.md`
- Length: `short` (240 words)
- Ideas:
1. Use `yield from` to delegate to a subgenerator and capture its final `return` value directly (e.g., `result = yield from subgen()`), instead of manually catching `StopIteration.value`.

## 011.emojis-in-python-with-pythonji
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/011.emojis-in-python-with-pythonji/item.md`
- Length: `short` (231 words)
- Ideas:
1. Use `pythonji` to experiment with emoji identifiers: install it with `python -m pip install pythonji`, save code in a `.🐍` file, and run it via `pythonji your_file.🐍` to execute emoji-based variable/class names.

## 012.at-operator-for-matrix-multiplication
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/012.at-operator-for-matrix-multiplication/item.md`
- Length: `short` (441 words)
- Ideas:
1. Overload the `@` operator in your own numeric-like classes by implementing `__matmul__`, `__rmatmul__`, and optionally `__imatmul__`, so expressions like `a @ b` and `a @= b` use your custom multiplication logic.

## 013.dunder-method-__eq__
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/013.dunder-method-__eq__/item.md`
- Length: `short` (729 words)
- Ideas:
1. In custom `__eq__`, return `NotImplemented` (not `False`) for unsupported types so Python can try reversed comparison and still guarantee `a == b` ends as a boolean.

## 014.reading-files-with-pathlib
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/014.reading-files-with-pathlib/item.md`
- Length: `short` (231 words)
- Ideas:
1. Use `pathlib.Path.read_text()` when you want newline normalization (`\r\n` becomes `\n`) and `Path.read_bytes()` when you need the exact raw file bytes, e.g. for cross-platform newline-sensitive processing.

## 016.quicksort-in-functional-programming
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/016.quicksort-in-functional-programming/item.md`
- Length: `short` (595 words)
- Ideas:
1. Use a `fixedpoint` helper to repeatedly apply a transformation until the value stops changing, e.g. `x2 = f(x); while x2 != x: x, x2 = x2, f(x2)` for convergence-style algorithms.

## 017.symmetrical-indexing-with-bitwise-inversion
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/017.symmetrical-indexing-with-bitwise-inversion/item.md`
- Length: `short` (473 words)
- Ideas:
1. Use `~i` as a mirror index in sequences (`~i == -i-1`) to compare symmetric elements cleanly, e.g. `for i in range(len(seq)//2): assert seq[i] == seq[~i]` for palindrome-style checks.

## 018.suppressing-errors-with-contextlib
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/018.suppressing-errors-with-contextlib/item.md`
- Length: `short` (489 words)
- Ideas:
1. Use `contextlib.suppress` to replace `try/except ...: pass` when an operation is optional (e.g., deleting a missing dict key or removing a non-existent list item), and pass only the exact exception type(s) you intentionally want to ignore.

## 019.sockets
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/019.sockets/item.md`
- Length: `short` (737 words)
- Ideas:
1. Use `recv()` returning `b''` as the reliable signal that the peer closed a TCP socket, so you can break your read loop and `close()` cleanly instead of waiting forever.

## 020.open-0
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/020.open-0/item.md`
- Length: `short` (213 words)
- Ideas:
1. Use `open(0).read()` to grab all piped or typed standard input at once (including multiline text), which is a handy shortcut for quick CLI scripts and REPL experiments.

## 022.python-module-selectors
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/022.python-module-selectors/item.md`
- Length: `short` (743 words)
- Ideas:
1. Use `selectors.DefaultSelector()` to build a single-threaded socket event loop: register each socket with `EVENT_READ` and store a handler in `data`, then call `selector.select()` and run `key.data(key.fileobj)` for ready sockets only (avoids blocking `accept()`/`recv()`).

## 023.how-to-use-namedtuple
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/023.how-to-use-namedtuple/item.md`
- Length: `short` (378 words)
- Ideas:
1. Use `collections.namedtuple` plus `Type._make` to turn CSV rows into lightweight records in one step: define `Person = namedtuple('Person', ['name', 'twitter'])`, then iterate with `for person in map(Person._make, csv.reader(f))` so you can access fields as `person.name` instead of tuple indexes.

## 024.cooking-with-asyncio
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/024.cooking-with-asyncio/item.md`
- Length: `medium` (2023 words)
- Ideas:
1. Show that calling an `async def` function returns a coroutine object, then demonstrate the correct entrypoint with `asyncio.run(main())` so readers avoid forgetting to actually execute coroutines.
2. Build a mini timing demo with `asyncio.gather()` to run two independent waits concurrently (for example 10s and 3s) and prove total runtime is near the longest task, not the sum.
3. Create a before/after snippet where `time.sleep()` inside `async def` blocks other tasks, then fix it with `await asyncio.sleep()` (or `await asyncio.to_thread(...)` for blocking work) to keep the event loop responsive.

## 025.reversed-emoji-flags
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/025.reversed-emoji-flags/item.md`
- Length: `short` (318 words)
- Ideas:
1. When reversing user-facing strings, don’t use `s[::-1]` on emoji text; split into grapheme clusters first (e.g., `import regex; clusters = regex.findall(r'\X', s); reversed_s = ''.join(reversed(clusters))`) so skin tones and flags stay intact.

## 026.web-apps-with-streamlit
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/026.web-apps-with-streamlit/item.md`
- Length: `short` (293 words)
- Ideas:
1. In Streamlit, wrap expensive CSV loading in a cached function (`@st.cache_data`) so widget interactions (sliders/checkboxes) rerun instantly without re-reading the file each time.

## 027.get-rich-with-python
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/027.get-rich-with-python/item.md`
- Length: `short` (656 words)
- Ideas:
1. Use `rich.inspect` as a supercharged `help()` in the REPL: `from rich import inspect; inspect(zip, help=True)` to get a colored, readable view of attributes, methods, and docs for any object.

## 028.disassemble-python
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/028.disassemble-python/item.md`
- Length: `short` (738 words)
- Ideas:
1. Use `dis.dis()` to sanity-check micro-optimizations: disassemble two function variants and confirm CPython already folds constants (e.g., `2+2` -> `LOAD_CONST 4`) or removes no-op statements, so you only optimize when bytecode actually changes.

## 029.true-is-equal-to-one
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/029.true-is-equal-to-one/item.md`
- Length: `short` (413 words)
- Ideas:
1. Use identity checks to distinguish booleans from integers in strict validations: `x is True`/`x is False` instead of `x == 1`/`x == 0`, because in Python `True == 1` and `False == 0`.

## 035.run-black-formatter-on-pre-commit
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/035.run-black-formatter-on-pre-commit/item.md`
- Length: `short` (319 words)
- Ideas:
1. Add a Git pre-commit hook that blocks unformatted code: create `.git/hooks/pre-commit` with `#!/bin/sh` and `exec black . --check`, then run `chmod +x .git/hooks/pre-commit`.

## 036.pandas-show-versions
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/036.pandas-show-versions/item.md`
- Length: `short` (454 words)
- Ideas:
1. When reporting a pandas bug, run `import pandas as pd; pd.show_versions(as_json='pandas-env.json')` to save a reproducible environment snapshot you can attach to the issue.

## 037.pandas-Series
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/037.pandas-Series/item.md`
- Length: `short` (366 words)
- Ideas:
1. In `pandas.Series`, duplicate index labels are allowed; use this to group quick lookups by label (e.g., `s = pd.Series([0,1,2], index=['a','b','a']); s['a']` returns both matching rows), unlike dictionaries where duplicate keys are overwritten.

## 042.how-to-do-beautiful-console-logging
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/042.how-to-do-beautiful-console-logging/item.md`
- Length: `short` (374 words)
- Ideas:
1. Use `rich.logging.RichHandler` in `logging.basicConfig(handlers=[RichHandler()])` with `FORMAT='%(message)s'` to instantly get colorized logs with timestamp, level, message, and source location, and call `log.exception(...)` inside `except` blocks to print pretty tracebacks automatically.

## 043.rich-traceback-and-exception-logging
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/043.rich-traceback-and-exception-logging/item.md`
- Length: `short` (524 words)
- Ideas:
1. Use `RichHandler(rich_tracebacks=True)` in `logging.basicConfig(...)` so exceptions logged with `logger.exception(...)` are rendered as colorized, easier-to-read Rich tracebacks.

## 044.pyscript
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/044.pyscript/item.md`
- Length: `short` (316 words)
- Ideas:
1. In a static HTML page, embed a `<py-script>` block to run Python on load and update a DOM node with `Element('target-id').write(...)`, e.g. writing a formatted `datetime.now()` timestamp into a `<span>`.

## 045.using-javascript-functions-in-pyscript
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/045.using-javascript-functions-in-pyscript/item.md`
- Length: `short` (424 words)
- Ideas:
1. In PyScript, call JavaScript timers from Python by importing `setInterval` from `js` and wrapping your Python callback with `pyodide.to_js`, e.g. update a DOM element every second without writing separate JavaScript.

## 046.custom-json-encoder
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/046.custom-json-encoder/item.md`
- Length: `short` (619 words)
- Ideas:
1. Subclass `json.JSONEncoder` and override `default()` to serialize unsupported types (e.g. `complex` to `{"real": obj.real, "imag": obj.imag}`), then pass `cls=YourEncoder` to `json.dumps()`/`json.dump()` while delegating unknown objects to `super().default(obj)` to preserve `TypeError` behavior.

## 047.custom-json-decoder
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/047.custom-json-decoder/item.md`
- Length: `medium` (806 words)
- Ideas:
1. Pass `object_hook=` to `json.loads` to turn specific dict shapes into Python objects, e.g. convert `{"real": 2, "imag": 3}` into `complex(2, 3)` during parsing.
2. Write your `object_hook` with EAFP: `try` reading required keys and `except KeyError` to return the original dict unchanged, so non-matching JSON objects decode normally.
3. Use one `object_hook` for whole payloads: it is called for every decoded JSON object (including nested ones), letting you recursively rebuild custom types without a separate traversal step.

## 048.custom-json-decoder-part-2
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/048.custom-json-decoder-part-2/item.md`
- Length: `short` (699 words)
- Ideas:
1. Tip ideas: subclass `json.JSONDecoder` and set a default `object_hook` in `__init__`; keep `object_hook` defensive (try keys, fall back to original dict); pass your decoder via `json.loads(..., cls=YourDecoder)` for symmetry with custom encoders; model domain objects (like complex numbers) from tagged/structured JSON dictionaries.

## 049.edit-multiple-file-locations-in-vscode
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/049.edit-multiple-file-locations-in-vscode/item.md`
- Length: `short` (129 words)
- Ideas:
1. In VS Code, use multi-cursor editing (`Ctrl` + click) to refactor repeated Python code quickly—for example, place cursors on several `print(` calls and change them all to `logger.info(` in one edit.

## 052.deleting-a-file-with-pathlib
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/052.deleting-a-file-with-pathlib/item.md`
- Length: `short` (167 words)
- Ideas:
1. Use `Path.unlink()` (not `remove`/`rm`) to delete files with `pathlib`, and guard it with `if path.is_file():` to avoid trying to unlink directories.

## 053.precision-of-python-floats
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/053.precision-of-python-floats/item.md`
- Length: `short` (374 words)
- Ideas:
1. Show that integers stop being exactly representable past 2**53 by checking `float(2**53) + 1 == float(2**53)` but `float(2**53) + 2` changes, then use this rule to avoid float-based IDs/counters for very large integers.

## 054.type-hint-Any
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/054.type-hint-Any/item.md`
- Length: `short` (600 words)
- Ideas:
1. Treat `typing.Any` as a temporary escape hatch: allow it at untyped boundaries, then immediately narrow with `isinstance` (or `cast`) inside the function so your checker regains type safety as you gradually replace `Any` with concrete types.

## 055.xkcd-plots
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/055.xkcd-plots/item.md`
- Length: `short` (631 words)
- Ideas:
1. Use `with plt.xkcd():` to apply the hand-drawn style to just one Matplotlib chart, and if you enabled it globally with `plt.xkcd()`, reset later plots with `plt.rcdefaults()`.

## 056.invisible-variables
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/056.invisible-variables/item.md`
- Length: `short` (310 words)
- Ideas:
1. Use `str.isidentifier()` plus `unicodedata.name()` to audit code for suspicious Unicode identifiers (including invisible Hangul filler characters) before `exec`/`eval` or code review.

## 057.optimising-images-for-the-web
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/057.optimising-images-for-the-web/item.md`
- Length: `short` (667 words)
- Ideas:
1. Use `Path.unlink(missing_ok=True)` when cleaning up generated files so your script can delete optional originals without wrapping each delete in `try/except FileNotFoundError`.

## 058.do-not-cache-generators
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/058.do-not-cache-generators/item.md`
- Length: `short` (218 words)
- Ideas:
1. When using `@lru_cache`, return a reusable collection (like `tuple(num**2 for num in range(n))`) instead of a generator, because cached generators are single-use iterators that come back already exhausted on later calls.

## 059.skip-list-data-structure
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/059.skip-list-data-structure/item.md`
- Length: `medium` (1433 words)
- Ideas:
1. Auto-adjust skip-list height with `max_lanes = ceil(log(length + 1))` before insertion so predecessor lookups cover every active lane.
2. In layered searches, reuse the last node found on a higher lane as the starting point for the next lower lane to avoid restarting from the head each time.
3. Implement predecessor discovery as a generator that yields from top lane to base lane, then reverse once (`list(_find(x))[::-1]`) to get insertion-ready order.

## 061.debugging-other-modules-in-vscode
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/061.debugging-other-modules-in-vscode/item.md`
- Length: `short` (268 words)
- Ideas:
1. In VS Code, add a second Python debug configuration in `launch.json` with `"justMyCode": false` (e.g., “Python: Current File + libs”) so you can choose it from the Run and Debug dropdown and step into third-party package code when troubleshooting.

## 063.skip-tests-on-microsoft-windows
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/063.skip-tests-on-microsoft-windows/item.md`
- Length: `short` (546 words)
- Ideas:
1. Use `pytestmark = pytest.mark.skipif(...)` at the top of a test module to skip an entire file for unsupported environments (for example, `sys.platform == 'win32'` or a specific `sys.version_info`), and add a `reason=` so skipped tests are self-explanatory in CI output.

## 064.open-recursion
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/064.open-recursion/item.md`
- Length: `short` (552 words)
- Ideas:
1. Use open recursion to make AST interpreters extensible: in a base `Interpreter.interpret`, recurse with `self.interpret(...)` (not a free function), and in subclasses handle new node types first then `return super().interpret(expr, env)` so nested new nodes are still dispatched correctly.

## 069.generator-membership-testing
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/069.generator-membership-testing/item.md`
- Length: `short` (470 words)
- Ideas:
1. `in` works on generators, but it consumes items as it searches; after `target in gen`, the generator is advanced (or exhausted), so for repeated membership checks or later reuse, materialize first (`items = list(gen)`) and test against `items` instead.

## 071.generator-method-close
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/071.generator-method-close/item.md`
- Length: `short` (392 words)
- Ideas:
1. Use `gen.close()` inside a `for` loop when you want to cancel future iterations but still run the rest of the current loop body (unlike `break`, which exits immediately).

## 072.read-bytecode-from-a-pyc-file
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/072.read-bytecode-from-a-pyc-file/item.md`
- Length: `short` (415 words)
- Ideas:
1. Disassemble a compiled `.pyc` directly by opening it in binary mode, skipping the header (`16` bytes on Python 3.6+), loading the code object with `marshal.load()`, and passing it to `dis.dis()` to inspect what Python actually executes.

## 073.customise-the-repl-on-start-up
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/073.customise-the-repl-on-start-up/item.md`
- Length: `short` (551 words)
- Ideas:
1. Set `PYTHONSTARTUP` to a `~/.pythonrc` file and use a guarded setup block (`try/except ImportError`) to preload REPL helpers like `rich.print` and `rich.traceback.install()`, so every `python` session gets your custom tools without failing on environments where optional packages are missing.

## 074.crazy-python-indentation
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/074.crazy-python-indentation/item.md`
- Length: `short` (701 words)
- Ideas:
1. Python tracks indentation by block level, not by a fixed number of spaces, so `if`/`else` blocks can align with different widths; add a formatter or run `python -m tabnanny your_file.py` to catch mixed tabs/spaces and keep indentation sane.

## 075.damerau-levenshtein-distance
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/075.damerau-levenshtein-distance/item.md`
- Length: `short` (645 words)
- Ideas:
1. Implement a typo-tolerant matcher by coding Damerau-Levenshtein distance with `@functools.lru_cache`, and include the adjacent-transposition rule (`a[-1]==b[-2] and a[-2]==b[-1]`) so swaps like `blal` vs `ball` count as a single edit.

## 076.hypothesis-for-code-refactoring
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/076.hypothesis-for-code-refactoring/item.md`
- Length: `short` (594 words)
- Ideas:
1. During a refactor, keep `old_fn` and `new_fn` side by side and use Hypothesis to generate inputs (`@given(...)`) that assert `old_fn(args) == new_fn(args)`; cap strategy sizes (e.g. `text(max_size=15)`) so the equivalence test stays fast.

## 077.piece-table-data-structure
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/077.piece-table-data-structure/item.md`
- Length: `medium` (1001 words)
- Ideas:
1. Implement `insert` and `delete` by routing both through one `replace(begin, end, text)` method (`insert(i, s) -> replace(i, i, s)`, `delete(a, b) -> replace(a, b, "")`) so all edit logic lives in one place.
2. Use a sentinel head node in linked text structures to remove special cases at index `0`, making split-and-splice operations uniform for beginning, middle, and end edits.
3. When editing large strings repeatedly, store text as linked spans (a piece table) and split only affected nodes at edit boundaries, then recompute downstream `start_idx` values instead of rebuilding the whole string each time.

## 078.context-variables
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/078.context-variables/item.md`
- Length: `medium` (1280 words)
- Ideas:
1. Define `ContextVar` objects at module top level (not inside functions) and always supply a safe fallback with `var.get("...")` to avoid `LookupError` when no value was set.
2. Show that `await some_coro()` keeps the same context but `asyncio.create_task(some_coro())` runs in a new context: set a `ContextVar` in each path and print values to verify isolation.
3. Avoid storing shared mutable objects (like lists) in a `ContextVar` when using multiple tasks; mutate-by-reference leaks across contexts, so set a fresh copy (`var.set(var.get().copy())`) before mutating.

## 079.textual-app-notifications
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/079.textual-app-notifications/item.md`
- Length: `short` (292 words)
- Ideas:
1. In Textual apps, call `self.notify(message, severity=..., timeout=...)` inside event handlers to show in-app toasts, using `severity='warning'` or `severity='error'` for emphasis and a custom `timeout` to keep important info visible longer.

## 080.how-to-draw-a-bezier-curve
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/080.how-to-draw-a-bezier-curve/item.md`
- Length: `short` (624 words)
- Ideas:
1. Implement quadratic Bézier sampling with De Casteljau’s algorithm: write a small `lerp(a, b, t)` helper and compute `p = lerp(lerp(start, control, t), lerp(control, end, t), t)` for `t` in `np.linspace(0, 1, 200)` (or a `for` loop) to draw smooth curves point by point.

## 082.20-million-lines-of-python-code
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/082.20-million-lines-of-python-code/item.md`
- Length: `short` (512 words)
- Ideas:
1. Avoid giant `if/elif` ladders for lookups: replace parity chains with `def is_even(n): return (n & 1) == 0`, then confirm the speedup with `timeit` on small and large inputs to catch branch-dependent slowdowns.

## 083.sentinel-value-for-default-arguments
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/083.sentinel-value-for-default-arguments/item.md`
- Length: `medium` (826 words)
- Ideas:
1. When `None` is a valid input, define a dedicated sentinel class (e.g., `class _Missing: pass`) and use a singleton instance as the default argument so omitted values are distinguishable from explicit `None`.
2. Do not annotate parameters with `object` just to support a sentinel default: `float | None | object` lets any value pass static type checking; use a narrow sentinel type instead.
3. In update-style APIs, gate assignments with sentinel checks so callers can partially update fields safely: `if not isinstance(total, _Missing): self.total = total` while still allowing `total=None` as meaningful input.

## 084.type-statement-and-type-aliases
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/084.type-statement-and-type-aliases/item.md`
- Length: `short` (485 words)
- Ideas:
1. Use Python 3.12’s `type` statement to define a recursive generic alias without string quotes, e.g. `type LinkedList[T] = T | tuple[T, LinkedList[T]]`, then annotate values with `LinkedList[int]` to get cleaner, lazily-evaluated forward references.

## 085.negative-zero
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/085.negative-zero/item.md`
- Length: `short` (539 words)
- Ideas:
1. Use `math.copysign` to detect negative zero reliably: `is_neg_zero = (x == 0.0 and math.copysign(1.0, x) < 0)`, which lets you preserve sign information when underflowed negatives become `-0.0`.

## 086.run-custom-code-on-python-start-up
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/086.run-custom-code-on-python-start-up/item.md`
- Length: `short` (232 words)
- Ideas:
1. Create a `sitecustomize.py` file inside your interpreter’s `site-packages` (find it with `python -c "import site; print(site.getsitepackages())"`) to run startup hooks automatically for every Python process, e.g. setting global warning filters or debug logging.

## 087.underscore-is-a-soft-keyword
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/087.underscore-is-a-soft-keyword/item.md`
- Length: `short` (256 words)
- Ideas:
1. In `match`/`case`, use `case _:` as a true wildcard fallback (soft keyword), but avoid expecting `_` to keep any value there since outside pattern matching it is just a normal variable name.

## 088.pythons-soft-keywords
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/088.pythons-soft-keywords/item.md`
- Length: `short` (592 words)
- Ideas:
1. Use `import keyword; keyword.softkwlist` in linters/code generators to reject context-sensitive names (`match`, `case`, `_`, `type`) only where they would be parsed as soft keywords, instead of banning them globally.

## 090.patching-module-globals-with-pytest
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/090.patching-module-globals-with-pytest/item.md`
- Length: `short` (437 words)
- Ideas:
1. When using `pytest.monkeypatch`, patch the name in the namespace your code reads at runtime: prefer `import constants` and `constants.FLAG` in production code, then `monkeypatch.setattr(constants, 'FLAG', True)` in tests instead of `from constants import FLAG`.

## 091.issue-user-warnings
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/091.issue-user-warnings/item.md`
- Length: `short` (463 words)
- Ideas:
1. Create a small `warn_deprecated()` helper that calls `warnings.warn(message, DeprecationWarning, stacklevel=2)` so warnings point to the caller’s line instead of the wrapper function.

## 092.dunder-method-__init_subclass__
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/092.dunder-method-__init_subclass__/item.md`
- Length: `short` (292 words)
- Ideas:
1. Use `__init_subclass__(cls, **kwargs)` to build a tiny plugin registry: accept a class keyword like `plugin_name='csv'` in subclass definitions, validate it at class creation time, and automatically store `cls` in a `Parent.plugins[plugin_name]` dict.

## 093.operator.methodcaller
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/093.operator.methodcaller/item.md`
- Length: `short` (456 words)
- Ideas:
1. Use `operator.methodcaller` to replace repetitive key lambdas, e.g. `sorted(strings, key=methodcaller('count', 'a'))` instead of `key=lambda s: s.count('a')` for cleaner reusable method-based keys.

## 094.pytest.raises-parameter-match
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/094.pytest.raises-parameter-match/item.md`
- Length: `short` (235 words)
- Ideas:
1. Use `pytest.raises(..., match=...)` with a regex to assert the exact error message (not just the exception type), e.g. `with pytest.raises(ValueError, match=r'^No negativity allowed!$'):` to ensure your test fails if a different `ValueError` path is triggered.

## 095.better-test-parametrisation-in-pytest
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/095.better-test-parametrisation-in-pytest/item.md`
- Length: `short` (531 words)
- Ideas:
1. In `pytest`, stack multiple `@pytest.mark.parametrize` decorators to generate a Cartesian product of inputs (e.g. `a` values x `b` values) instead of manually listing every pair.

## 097.get-terminal-size
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/097.get-terminal-size/item.md`
- Length: `short` (173 words)
- Ideas:
1. Use `shutil.get_terminal_size(fallback=(120, 30))` at startup and read `size.columns` to dynamically wrap/truncate CLI output, so your script still formats cleanly when run without an attached TTY.

## 099.order-values-of-dictionary-by-iterable-of-keys-with-operator.itemgetter
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/099.order-values-of-dictionary-by-iterable-of-keys-with-operator.itemgetter/item.md`
- Length: `short` (321 words)
- Ideas:
1. Precompile a reusable selector with `operator.itemgetter(*keys)` to fetch dictionary values in a custom key order across multiple mappings (e.g., `pick = itemgetter(*order); pick(d1), pick(d2)`), then `zip` the results to align related fields quickly.

## 100.making-an-iterator-out-of-a-function
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/100.making-an-iterator-out-of-a-function/item.md`
- Length: `short` (332 words)
- Ideas:
1. Replace `while True` + `break` input loops with `for text in iter(input, 'exit'):` to stop automatically when the sentinel value is entered.

## 101.difference-between-__getattr__-and-__getattribute__
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/101.difference-between-__getattr__-and-__getattribute__/item.md`
- Length: `short` (248 words)
- Ideas:
1. Use `__getattr__` (not `__getattribute__`) to provide fallback values for missing attributes, and reserve `__getattribute__` for advanced cases where you explicitly delegate to `object.__getattribute__(self, name)` to avoid infinite recursion.

## 102.ctrl-left-and-ctrl-right-not-working-in-the-python-repl-on-macos
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/102.ctrl-left-and-ctrl-right-not-working-in-the-python-repl-on-macos/item.md`
- Length: `short` (186 words)
- Ideas:
1. On macOS, if `Ctrl+Left/Right` doesn’t jump by words in the Python 3.13+ REPL, disable Mission Control’s space-switch shortcuts (`Settings → Keyboard Shortcuts → Mission Control` and untick `Move left a space` / `Move right a space`) so those keybindings reach Python.

## 103.debugging-the-new-python-repl-with-trace-and-pyrepl-trace
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/103.debugging-the-new-python-repl-with-trace-and-pyrepl-trace/item.md`
- Length: `short` (231 words)
- Ideas:
1. When hacking on Python 3.13+ REPL internals, log safely with `from _pyrepl import trace; trace.trace('msg')` and launch `PYREPL_TRACE=/tmp/repl.log python` while running `tail -f /tmp/repl.log` in another terminal to watch live debug output without polluting the REPL screen.

## 105.pytest-selection-arguments-for-failing-tests
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/105.pytest-selection-arguments-for-failing-tests/item.md`
- Length: `medium` (1004 words)
- Ideas:
1. Speed up bug-fix loops by rerunning only previously failing tests with `pytest --last-failed -vv`, so you avoid waiting for the full suite.
2. Work through failures one at a time with `pytest --stepwise`; it resumes from the last failing test on each rerun, and removing `.pytest_cache` resets progress.
3. Prioritize known breakages while still checking everything by running `pytest --failed-first --maxfail=1`, which runs old failures first and stops quickly on the next failure.

## 106.asyncio-gather
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/106.asyncio-gather/item.md`
- Length: `short` (111 words)
- Ideas:
1. Show that `asyncio.gather` preserves input order (not completion order) by awaiting tasks with different `sleep` times, then rely on positional unpacking like `user, posts, comments = await asyncio.gather(...)` to keep results mapped correctly.

## 107.automatically-activating-virtual-environments
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/107.automatically-activating-virtual-environments/item.md`
- Length: `short` (272 words)
- Ideas:
1. Detect whether your script is running inside a virtual environment with `if sys.prefix == sys.base_prefix: print('Activate .venv first')` so tools fail fast when you forget to activate the project environment.

## 108.date-sequences-in-polars
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/108.date-sequences-in-polars/item.md`
- Length: `short` (309 words)
- Ideas:
1. Use `pl.date_range(start, end, interval="1mo2d", eager=True)` to generate calendar-aware recurring dates (e.g., billing cycles) so month jumps respect real calendar months instead of fixed day counts.

## 109.unpacking-kwargs-with-custom-objects
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/109.unpacking-kwargs-with-custom-objects/item.md`
- Length: `short` (224 words)
- Ideas:
1. Make a lightweight settings object callable with `**` by implementing `keys()` and `__getitem__()`, then pass it directly to functions like `connect(**settings_obj)` without converting to a dict first.

## 110.hash-of-1-is-2
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/110.hash-of-1-is-2/item.md`
- Length: `short` (317 words)
- Ideas:
1. When implementing `__hash__`, avoid returning `-1`: Python reserves it internally and will convert it to `-2`, so use any other stable integer to prevent an unexpected collision with objects whose hash is already `-2`.

## 111.interrupting-scripts-without-tracebacks
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/111.interrupting-scripts-without-tracebacks/item.md`
- Length: `short` (282 words)
- Ideas:
1. Wrap your CLI entrypoint in `with contextlib.suppress(KeyboardInterrupt): main()` so users can stop long-running scripts with Ctrl+C without seeing a traceback.

## 112.re-match-groupdict
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/112.re-match-groupdict/item.md`
- Length: `short` (190 words)
- Ideas:
1. Parse structured strings with named regex groups and instantiate objects in one step: `match = pattern.match(text)` then `MyDataClass(**match.groupdict())` to map fields without manual indexing.

## 113.standalone-executable-python-scripts-with-uv
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/113.standalone-executable-python-scripts-with-uv/item.md`
- Length: `short` (471 words)
- Ideas:
1. Turn a single-file script into a portable CLI by running `uv add <package> --script your_script.py`, adding `#!/usr/bin/env -S uv run` as the first line, then `chmod +x your_script.py` so it self-manages dependencies when executed.

## 114.implicit-casting-in-dataframe-concatenation
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/114.implicit-casting-in-dataframe-concatenation/item.md`
- Length: `short` (320 words)
- Ideas:
1. In Polars, when vertical concatenation fails due to dtype mismatches (for example `Int64` vs `UInt8`), use `pl.concat([df1, df2], how="vertical_relaxed")` to auto-cast to a compatible common type instead of manually casting columns first.

## 115.nested-easter-eggs
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/115.nested-easter-eggs/item.md`
- Length: `short` (275 words)
- Ideas:
1. Use `inspect.getsource(antigravity.geohash)` in the REPL after `import antigravity` to discover hidden behavior in standard-library modules and learn by reading real CPython code.

## 117.publishing-a-python-package-with-uv
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/117.publishing-a-python-package-with-uv/item.md`
- Length: `short` (288 words)
- Ideas:
1. Before publishing with `uv`, run `uv build` locally, then publish with a temporary broad PyPI token (`uv publish --token ...`) and immediately rotate to a project-scoped token for safer future releases.

## 121.use-pip-directly-from-a-uv-virtual-environment
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/121.use-pip-directly-from-a-uv-virtual-environment/item.md`
- Length: `short` (197 words)
- Ideas:
1. If `python -m pip` fails inside a `uv` environment (`No module named pip`), recreate it with `uv venv --seed` so the virtual environment includes `pip` and supports standard `venv` + `pip` commands.

## 122.cli-tools-with-extra-dependencies
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/122.cli-tools-with-extra-dependencies/item.md`
- Length: `short` (152 words)
- Ideas:
1. Use `uv tool install --with <extra_pkg> <cli_tool>` to bundle runtime dependencies into a tool’s isolated environment (e.g., `uv tool install --with pandas --with polars marimo`) so the CLI works with those libraries without polluting your global Python setup.

## 123.the-appearing-builtin
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/123.the-appearing-builtin/item.md`
- Length: `short` (403 words)
- Ideas:
1. In the Python REPL, if you assign to `_` for your own use, you can still access the real last expression result via `import builtins; builtins._` because your assignment only shadows the special REPL `_` name.

## 124.thousands-separators
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/124.thousands-separators/item.md`
- Length: `short` (126 words)
- Ideas:
1. Use f-string specifiers like `:_b`, `:_o`, and `:_x` to auto-insert underscores in binary/octal/hex output, making bitmasks and hex constants readable in 4-digit groups (e.g., `f"{value:_x}"`).

## 126.hash-of-infinity
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/126.hash-of-infinity/item.md`
- Length: `short` (100 words)
- Ideas:
1. Use `hash(float('inf'))` and `hash(float('-inf'))` in a REPL to sanity-check Python’s special float hashing (`314159` and `-314159`) when teaching or debugging how numeric hashes behave for edge-case values.

## 127.dynamically-changing-the-type
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/127.dynamically-changing-the-type/item.md`
- Length: `short` (167 words)
- Ideas:
1. Use `obj.__class__ = NewType` to switch an instance to another compatible custom class at runtime (same attribute layout), so you can change behavior without recreating the object.

## 128.matching-prefixes-and-suffixes-with-regex
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/128.matching-prefixes-and-suffixes-with-regex/item.md`
- Length: `short` (239 words)
- Ideas:
1. Use `\B` with one-sided boundaries to target affixes precisely: `r"\blegal\B"` finds words starting with `legal` (e.g. `legally`), while `r"\Blegal\b"` finds words ending with `legal` (e.g. `illegal`).

## 130.format-python-code-directly-with-uv
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/130.format-python-code-directly-with-uv/item.md`
- Length: `short` (186 words)
- Ideas:
1. Use `uv format --diff` to preview Ruff formatting changes before applying them, then run `uv format` to format the whole project from the current directory once the diff looks good.

## 131.change-casing-in-search-and-replace
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/131.change-casing-in-search-and-replace/item.md`
- Length: `short` (415 words)
- Ideas:
1. Use a callable replacement in `re.sub` to capitalize only the first character of each regex match (e.g., `lambda m: m.group(0)[:1].upper() + m.group(0)[1:]`) when Python replacement strings can’t do VS Code-style `\u`/`\U` case transforms.

## 134.-alignment-in-string-formatting
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/134.-alignment-in-string-formatting/item.md`
- Length: `short` (215 words)
- Ideas:
1. Use sign-aware numeric padding in f-strings with `=` (or `0` before width), e.g. `f"{n:=8}"` / `f"{n:08}"`, to keep `+`/`-` at the left edge while padding digits for neat ledgers.

## 135.build-the-python-documentation
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/04.til/135.build-the-python-documentation/item.md`
- Length: `short` (261 words)
- Ideas:
1. When contributing to CPython docs, run `make -C Doc venv htmllive` to preview changes with live reload and quickly catch reStructuredText indentation bugs (like an over-indented `.. XXX` comment inside `.. seealso::`).

## 5-ways-to-flatten-a-list-of-lists
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/5-ways-to-flatten-a-list-of-lists/item.md`
- Length: `medium` (942 words)
- Ideas:
1. Prefer a nested list comprehension to flatten one level: `flat = [x for sub in list_of_lists for x in sub]`.
2. Avoid `sum(list_of_lists, [])` (and `reduce(add, ...)`) for flattening lists, because repeated list concatenation does extra work and scales poorly.
3. Use `itertools.chain.from_iterable(list_of_lists)` when you want lazy flattening, so you can iterate and stop early without building a full list.

## 50-shades-of-sign
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/50-shades-of-sign/item.md`
- Length: `long` (3225 words)
- Ideas:
1. Use `dis.dis()` to check whether two refactors are actually equivalent at the bytecode level before debating style differences.
2. For tri-state outputs, exploit boolean arithmetic: compute sign-like values with `(x > 0) - (x < 0)`.
3. When micro-optimizing, benchmark with `timeit` on representative inputs (positive, negative, zero, ints, and floats) instead of guessing.
4. Flatten control flow with an early return for edge cases (e.g. `if x == 0: return 0`) and keep the main path linear.
5. Use truthiness intentionally (`if not x`) for zero checks only when your input domain is numeric and that behavior is clearly documented.

## 503-days-working-full-time-on-foss-lessons-learned
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/503-days-working-full-time-on-foss-lessons-learned/item.md`
- Length: `long` (4135 words)
- Ideas:
1. Add a `python -m yourpackage.diagnose` command that prints Python version, OS, and key dependency versions so bug reporters can paste reproducible environment details.
2. Require a *minimal reproducible example* in bug reports: include a tiny script template in your repo that users can copy, run, and attach when opening issues.
3. Open feature PRs as draft first, then do a self-review pass on GitHub before requesting reviewers to catch naming, edge-case, and clarity issues early.
4. Run your full test suite after every “small” change (even typos/refactors) and automate it with a local pre-push hook to avoid surprising CI failures.
5. Keep a per-issue debugging note (hypotheses tried, failed fixes, observations) so complex bug hunts stay structured and easier to hand off or revisit.

## a-generator-duck-typing-and-a-branchless-conditional-walk-into-a-bar
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/a-generator-duck-typing-and-a-branchless-conditional-walk-into-a-bar/item.md`
- Length: `long` (4577 words)
- Ideas:
1. Count matches without an `if` block: use `sum(predicate(x) for x in iterable)` because `True`/`False` sum as `1`/`0`.
2. When aggregating values, pass a generator expression directly (e.g. `sum(x*x for x in data)`) instead of building a temporary list first.
3. Design functions around behavior, not class names: require methods like `.swim()` and `.dive()` so any object with that interface works.
4. Write utility functions against capabilities (e.g. indexing with `obj[0]` or iteration with `enumerate(obj)`) so they work with lists, tuples, strings, ranges, and generators.
5. Flatten conditional string-building by always concatenating fragments and using identity values (`""`) for irrelevant parts, rather than many `if` branches.

## accepting-the-psf-community-service-award
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/accepting-the-psf-community-service-award/item.md`
- Length: `medium` (803 words)
- Ideas:
1. Store conference timestamps in UTC and convert for attendees with `zoneinfo.ZoneInfo` (e.g., Lisbon -> New York) so PyCon schedule emails always show correct local times.
2. Generate clean announcement previews with `textwrap.shorten(text, width=120, placeholder='…')` to create social snippets from long community posts without cutting words mid-sentence.
3. Use `urllib.parse.urlsplit()` to validate and inspect shared award/post links, then read parts like `netloc`, `path`, and fragment to route links by platform.

## adding-stats-to-my-blog
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/adding-stats-to-my-blog/item.md`
- Length: `medium` (1109 words)
- Ideas:
1. When processing Markdown files, strip optional YAML frontmatter first: check `text.startswith('---')`, then remove everything up to the next `'---'` so metadata doesn’t pollute word/code stats.
2. Extract and remove fenced code blocks in one pass with `re.sub` + a callback: append each `match.group(0)` to a list and return `''` to leave behind prose-only text.
3. Estimate lines of code from Markdown fences by summing `len(block.splitlines())` for each extracted snippet and subtracting `2 * n_blocks` to ignore opening/closing ``` fence lines.

## advent-of-code-sonar-sweep-analysis
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/advent-of-code-sonar-sweep-analysis/item.md`
- Length: `long` (4571 words)
- Ideas:
1. Simplify sliding-window comparisons algebraically: for equal-size overlapping windows, comparing sums is equivalent to comparing only the exiting vs entering element (e.g., `depths[i-3] < depths[i]`).
2. Use `zip(seq, seq[1:])` to compare consecutive items without manual indices; rely on `zip` stopping at the shortest iterable so no end-boundary checks are needed.
3. Convert file lines to integers in one pass with `depths = [int(line) for line in f]` to avoid a second traversal and repeated `int(...)` calls later.
4. Remember `int()` accepts surrounding whitespace, so `int(line)` works directly on lines like `'  42\n'` without calling `.strip()` first.
5. For large inputs, compare items `k` apart lazily by splitting one iterator and offsetting one branch: `a, b = tee(data); b = islice(b, k, None); count = sum(x < y for x, y in zip(a, b))`.

## animating-a-rotating-spiral
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/animating-a-rotating-spiral/item.md`
- Length: `short` (610 words)
- Ideas:
1. Animate any parametric curve by adding a `time` phase offset inside `sin`/`cos` (e.g., `cos(theta + t)`) and iterating frames with `itertools.count(step=0.1)` to get smooth rotation without storing state.

## animating-a-tree-fractal
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/animating-a-tree-fractal/item.md`
- Length: `medium` (1154 words)
- Ideas:
1. Use `yield from` in recursive functions to build fractal line segments lazily, then wrap with `list(...)` only when you need to reuse the full geometry across frames.
2. Keep all geometry math in a standard Cartesian system and do a single coordinate conversion at draw time (center offset + flipped y-axis) to simplify recursive logic.
3. Animate static shapes by interpolating one `alpha` parameter and applying an affine transform pipeline (rotate, scale, translate) to every segment each frame.

## animations-from-first-principles-in-5-minutes
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/animations-from-first-principles-in-5-minutes/item.md`
- Length: `medium` (2222 words)
- Ideas:
1. Use frame-based progress for smooth, predictable animations: set `FRAMES = FPS * DURATION`, then compute `alpha = frame / FRAMES` inside the loop instead of relying on wall-clock timing.
2. Represent shapes as callables `shape(progress) -> (x, y)` and morph them with linear interpolation: compute points from both shapes at the same `progress`, then return `(x1 + (x2-x1)*alpha, y1 + (y2-y1)*alpha)`.
3. Generate color gradients from animation parameters: write a `colour(t)` function (using `sin`/`cos`) and pass `colour(progress)` for along-curve gradients or `colour(alpha)` for across-morph color transitions.

## ascii-rain-scroll-art
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/ascii-rain-scroll-art/item.md`
- Length: `short` (635 words)
- Ideas:
1. Use `collections.deque(maxlen=height)` as a scrolling frame buffer: `appendleft(new_row)` inserts the newest terminal line at the top and automatically drops the oldest bottom line, so you can animate text without manual list slicing.

## automatic-site-updates-with-cog-and-pre-commit
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/automatic-site-updates-with-cog-and-pre-commit/item.md`
- Length: `short` (659 words)
- Ideas:
1. Use a local `pre-commit` hook with `pass_filenames: false` to run `cog -r` on a single Markdown file and auto-`git add` it, so generated sections (like stats tables) are always refreshed before each commit.

## automatically-pushing-code-changes-during-live-coding-with-uv
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/automatically-pushing-code-changes-during-live-coding-with-uv/item.md`
- Length: `short` (405 words)
- Ideas:
1. Turn a Python utility into a portable one-file CLI by adding the `uv` shebang and inline `# /// script` dependencies, then run it from any repo (using `os.getcwd()`) without creating project-specific virtual environments.

## base-conversion-in-python
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/base-conversion-in-python/item.md`
- Length: `medium` (1434 words)
- Ideas:
1. Parse mixed-base user input with `int(value, 0)` so Python auto-detects `0b`, `0o`, and `0x` prefixes (e.g. `int('0x49', 0) == 73`).
2. When you need base digits without prefixes, format directly: `f'{n:b}'`, `f'{n:o}'`, `f'{n:x}'` and iterate those characters instead of slicing `bin()/oct()/hex()` output.
3. For bases beyond 36, represent numbers as digit lists and convert with repeated division (`to_base`) and positional accumulation (`from_base`) since `int(..., base=...)` only supports bases 2..36.

## beating-linkedin-queens-with-python
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/beating-linkedin-queens-with-python/item.md`
- Length: `long` (4771 words)
- Ideas:
1. Speed up brute-force solvers by pruning early: after a tentative move, rebuild each remaining candidate set and `continue` immediately if `not all(new_group_sets)`.
2. Count grid cells in an image by scanning one line and using `itertools.groupby` on `is_black(pixel)`; the number of non-black runs equals the number of cells.
3. Bias 2D search toward the top-left by yielding coordinates in increasing Chebyshev distance (an `expanding_range` generator) instead of row-by-row scanning.
4. Write an image-mode-agnostic `is_black` helper that accepts grayscale ints, normalized floats, and RGB/RGBA tuples, ignoring alpha for consistent threshold checks.
5. Convert board coordinates to pixel centers with integer math: compute `dx, dy` from grid corners and use `left + x*dx + dx//2`, `top + y*dy + dy//2` for stable cell sampling.

## biggest-square
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/biggest-square/item.md`
- Length: `medium` (1304 words)
- Ideas:
1. When scanning a grid for the best candidate, get top-most/left-most tie-breaking for free by iterating rows top-to-bottom and columns left-to-right, and only replacing the best result when `new_size > best_size` (not `>=`).
2. To grow a candidate square efficiently, check only the newly added bottom row and right column at each step instead of re-validating the whole square every time.
3. Prune impossible starts early: once your current best square has size `k`, skip positions where `x + k >= width` or `y + k >= height` because no larger square can fit from there.

## binary-search-as-a-bidirectional-generator
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/binary-search-as-a-bidirectional-generator/item.md`
- Length: `medium` (1418 words)
- Ideas:
1. Build an ergonomic interactive search loop by yielding a guess, receiving feedback via `send(...)`, and adding a bare `yield` right after validation so `for guess in searcher:` and `searcher.send(feedback)` work cleanly together.
2. Replace ambiguous numeric control signals (`-1`, `0`, `1`) with an `Enum` like `TOO_LOW`, `TOO_HIGH`, and `HIT`, then validate `isinstance(feedback, FeedbackType)` to make generator-driven algorithms self-documenting and safer.
3. For unknown upper bounds, do exponential probing first (`delta *= 2` while feedback is `TOO_LOW`), then hand off to bounded binary search with `yield from` to keep bidirectional `send` communication intact across both phases.

## breaking-out-of-nested-loops-with-generators
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/breaking-out-of-nested-loops-with-generators/item.md`
- Length: `short` (416 words)
- Ideas:
1. Extract nested traversal into a small generator (or `itertools.chain.from_iterable`) so your search loop is single-level and `break` stops immediately at the first match.

## building-a-python-compiler-and-interpreter-02-numbers
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/building-a-python-compiler-and-interpreter-02-numbers/item.md`
- Length: `long` (2911 words)
- Ideas:
1. Make `__repr__` refactor-proof by using `self.__class__.__name__` instead of hardcoding class names, so renamed classes still print correctly.
2. In tokenizers, extract number scanning into a helper like `consume_int()` that advances the pointer through all digits, then return `int(code[start:ptr])`.
3. Accept floats with missing fractional digits (`73.`) by parsing the decimal slice and defaulting `'.'` to `'.0'` before calling `float()`.
4. Prevent invalid float tokens by requiring lookahead for `.`: only tokenize a leading dot as float when the next character is a digit.
5. Reduce parser duplication by introducing `parse_number()` and `parse_computation()` helpers, then keep `parse()` as a small entrypoint that just parses and checks `EOF`.

## building-a-python-compiler-and-interpreter-03-visitor-pattern
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/building-a-python-compiler-and-interpreter-03-visitor-pattern/item.md`
- Length: `long` (3771 words)
- Ideas:
1. Define your language grammar directly in the parser docstring and implement one `parse_<rule>()` method per grammar rule to keep parser changes systematic.
2. Parse left-associative operator chains (like `1 - 2 + 3`) with a loop: start from the first operand, then repeatedly wrap `result = BinOp(op, result, next_operand)`.
3. Create a shared AST base like `Expr` so nodes such as `Int`, `Float`, and `BinOp` can be typed uniformly and nested without type errors.
4. Use a visitor-style dispatcher with `getattr(self, f"compile_{type(node).__name__}")` to route each AST node to its dedicated compiler method.
5. In tests, add a small `run_computation(code)` helper that tokenizes, parses, compiles, and interprets, then assert only the final value for concise end-to-end coverage.

## building-a-python-compiler-and-interpreter-04-arithmetic
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/building-a-python-compiler-and-interpreter-04-arithmetic/item.md`
- Length: `long` (4630 words)
- Ideas:
1. When tokenizing overlapping operators, always match the longest one first: check for `"**"` before `"*"` so exponentiation isn’t split into two multiplication tokens.
2. Model operator precedence in recursive-descent parsers by grammar depth: put `term` between `computation` and `unary` so `* / %` bind tighter than `+ -`.
3. Parse right-associative exponentiation correctly with recursion on the right side (`exponentiation := atom EXP unary | atom`) so `2 ** 3 ** 4` becomes `2 ** (3 ** 4)`.
4. Add a dynamic safety test that iterates all `BytecodeType` values and asserts `Interpreter` has matching `interpret_<name>` methods to catch missing opcode handlers early.
5. Treat parentheses as parse-time structure, not AST nodes: parse `LPAREN computation RPAREN` into the inner expression directly to keep the AST simpler.

## Run summary
- Selected: 204
- Succeeded: 114
- Failed: 90
- Retries used: 91
- Failed slugs: building-a-python-compiler-and-interpreter-05-statements,building-a-python-compiler-and-interpreter-06-variables building-a-python-compiler-and-interpreter-07-if,building-a-python-compiler-and-interpreter-08-booleans building-a-python-compiler-and-interpreter-09-short-circuiting,building-a-python-compiler-and-interpreter-10-elif-and-else building-a-python-compiler-and-interpreter,case-insensitive-dictionary case-insensitive-string-class,chatroom-server-tutorial connecting-python-and-dyalog-apl-with-sockets,context-managers-with-contextlib-contextmanager contributing-to-open-source-rich-2073,counting-passwords-with-automatons creating-programming-language,custom-json-encoder-and-decoder customising-object-creation-with-__new__,deducing-physics-formulas-with-genetic-algorithms dipping-my-toes-in-metaprogramming,divmod-for-unit-conversions dynamic-string-replacements-with-regex,elo-rating-system-simulation finding-and-fixing-over-10000-bugs-on-github,finding-similar-photos floodfill-algorithm-in-python,functools-cmp_to_key functools-partial,functools-placeholder generalising-itertools-pairwise,hold-my-parentheses-light-now-with-zero-sugar hold-my-parentheses,how-to-create-a-python-package-in-2022 how-to-work-with-case-insensitive-strings,huehue ignoring-exceptions-with-contextlib-suppress,itertools-batched julia-set,learn-pandas-and-matplotlib-with-pokemon longest-unique-substring,lsbasi-apl-part1 lsbasi-apl-part2,lsbasi-apl-part3 lsbasi-apl-part4,mandelbrot manipulate-boolean-values-with-conditions,max-is-broken minesweeper-remake,minimax-algorithm-and-alpha-beta-pruning module-collections-overview,module-compression-overview module-enum-overview,module-itertools-overview module-pathlib-overview,more-animations-from-first-principles-in-5-minutes mutability-and-random-shuffle,n-queens-problem parsing-txrx-files-in-python,personal-highlights-of-europython-2025 personal-highlights-of-pycon-italy-2025,personal-highlights-of-pycon-us-2025 pigeon-pooping-simulator,pycon-portugal-2025-programme-mafia python-deque-tutorial,python-quiz-europython-2025-edition python-quiz-pycon-italia-2025-edition,random-walk-simulations recursive-structural-pattern-matching,regex-in-real-life regex-printer,reverse-engineering-the-chronospatial-computer running-python-inside-chatgpt,secret-santa-draw-with-python sets-as-dictionaries-with-no-values,single-line-calculator sockets-for-dummies,solving-bridge-repair-in-4ms-with-python solving-mazes-with-programming,solving-wordle-with-python teaching-the-worlds-largest-programming-lesson,the-formula-that-plots-itself using-an-llm-to-write-a-countdown-timer,using-uv-to-build-and-install-python-cli-apps uv-cheatsheet,water-buckets what-is-args-and-how-do-you-use-it,who-wants-to-be-a-millionaire why-apl-is-a-language-worth-knowing,why-use-enums writing-interpreter-in-15-loc,yamlutils


# Idea Generation Run (2026-02-16 14:39:33)


# Idea Generation Run (2026-02-23 15:11:03)

## building-a-python-compiler-and-interpreter-05-statements
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/building-a-python-compiler-and-interpreter-05-statements/item.md`
- Length: `long` (2619 words)
- Ideas:
1. Use a `beginning_of_line` flag in a tokenizer to emit `NEWLINE` tokens only after real code, so consecutive blank lines are ignored automatically.
2. Append a sentinel `"\n"` to source code inside your tokenizer (`self.code = code + "\n"`) to simplify grammars that require statements to end with a newline.
3. When grammar changes break parser tests, test lower-level parser methods directly (for example `parse_computation()`) instead of always asserting through the top-level `parse()` entry point.
4. Model expression statements with a dedicated AST node like `ExprStatement(expr)` instead of making every expression node a statement, which keeps the AST hierarchy accurate.
5. In a stack-based interpreter/compiler, compile standalone expression statements with an explicit `POP` instruction so intermediate results do not accumulate on the VM stack.

## building-a-python-compiler-and-interpreter-06-variables
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/building-a-python-compiler-and-interpreter-06-variables/item.md`
- Length: `long` (3824 words)
- Ideas:
1. When tokenizing identifiers, define two character sets (`LEGAL_NAME_START_CHARACTERS` and `LEGAL_NAME_CHARACTERS`) so you can enforce different rules for the first character vs the rest.
2. In a recursive-descent parser, use one-token lookahead (e.g. check whether the next token is `ASSIGN`) to decide between parsing an expression statement or an assignment.
3. Represent variable access as an expression node (like `Variable(name)`) so the same AST node works both on the left side of assignments and inside computations.
4. Compose compiler passes with generator methods and `yield from` (e.g. compile the RHS first, then emit `SAVE`) to keep bytecode generation small and readable.
5. For chained assignments in a stack-based interpreter/compiler (`a = b = c = 3`), emit a `COPY`/dup operation before each save except the last so the value is evaluated only once.

## building-a-python-compiler-and-interpreter-07-if
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/building-a-python-compiler-and-interpreter-07-if/item.md`
- Length: `long` (4515 words)
- Ideas:
1. When tokenizing identifiers, parse the name first and then remap it through a `keywords` dictionary (e.g. `{'if': TokenType.IF}`) instead of hardcoding keyword checks all over the tokenizer.
2. For Python-like indentation parsing, emit only indentation *changes* (`INDENT`/`DEDENT`) by tracking the current indent level, instead of emitting indentation tokens for every indented line.
3. Use a `collections.deque` as a token buffer when one tokenizer step can generate multiple tokens (like multiple `DEDENT`s) but your `next_token()` API must return just one token at a time.
4. If your bytecode needs jumps (like `if` statements), switch the interpreter loop from `for bc in bytecode` to `while ptr < len(bytecode)` so each instruction handler can control the instruction pointer.
5. To compile an `if` block, compile the body first to measure its bytecode length, then emit a `POP_JUMP_IF_FALSE` with the jump offset (`len(body_bytecode) + 1`) before yielding the body instructions.

## building-a-python-compiler-and-interpreter-08-booleans
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/building-a-python-compiler-and-interpreter-08-booleans/item.md`
- Length: `medium` (1949 words)
- Ideas:
1. Use `keyword.iskeyword()` to verify language keywords (e.g. `True` and `False` are keywords in Python), especially when writing tokenizers or linters.
2. When building an AST for a parser/compiler, replace separate literal nodes (`Int`, `Float`, `Bool`) with a single `Constant` node to simplify parsing, compilation, and future support for new literal types.
3. Remember that `not` has lower precedence than arithmetic (`not 1 + 2` means `not (1 + 2)`); in a recursive-descent parser, add a `negation` rule above arithmetic instead of modifying your math rules.

## building-a-python-compiler-and-interpreter-09-short-circuiting
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/building-a-python-compiler-and-interpreter-09-short-circuiting/item.md`
- Length: `long` (3020 words)
- Ideas:
1. Use `dis.dis()` on expressions like `a and b` or `a or b` to see Python's short-circuiting bytecode (`POP_JUMP_IF_FALSE` / `POP_JUMP_IF_TRUE`) and debug evaluation order.
2. Remember that `and` and `or` return one of their operands (not necessarily `True`/`False`), so `0 or 10` gives `10` and `1 and 2` gives `2`.
3. When writing a parser, encode Boolean precedence with separate rules (`or` over `and` over `not`) instead of handling all Boolean operators in one parsing function.
4. If you're building an AST transformer/compiler, represent chained Boolean expressions as a single node with a list of values (like `a and b and c`) to simplify short-circuit code generation.
5. Generate compact truth-table tests for Boolean precedence with `itertools.product([True, False], repeat=3)` and compare your evaluator against Python's result for expressions like `a and b or c`.

## building-a-python-compiler-and-interpreter-10-elif-and-else
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/building-a-python-compiler-and-interpreter-10-elif-and-else/item.md`
- Length: `medium` (1531 words)
- Ideas:
1. Model optional branches in an AST with a dataclass field like `orelse: Body | None = None` to represent statements (`if`) that may or may not have a fallback block.
2. Implement `elif` without a new AST node by desugaring it into a nested `if` inside the previous node’s `orelse`, chaining multiple `elif`s in a loop.
3. When compiling `if/else` to bytecode, emit a conditional jump to skip the `if` body on false and append an unconditional forward jump at the end of the `if` body to skip the `else` block.

## building-a-python-compiler-and-interpreter
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/building-a-python-compiler-and-interpreter/item.md`
- Length: `long` (7009 words)
- Ideas:
1. Use `dis.dis()` on a small function (for example, `return a + b`) to inspect Python bytecode and map source code operations to instructions like `LOAD_FAST`, `BINARY_OP`, and `RETURN_VALUE`.
2. In a hand-written parser, add `eat(expected_type)` and `peek()` helpers: `eat` validates and consumes the next token, while `peek` lets you branch on upcoming token types without consuming them.
3. After parsing an expression, explicitly consume an `EOF` token to ensure the whole input was parsed and catch trailing garbage like `3 + 5 - 2` when only one binary operation is allowed.
4. Make a tokenizer iterable by implementing `__iter__` as a generator that repeatedly yields `next_token()` and includes the final `EOF` token, so you can simply call `list(Tokenizer(code))` in tests.
5. Replace repetitive tokenizer tests with `pytest.mark.parametrize` (e.g., `+`, `-`, `3`) so one test function checks multiple token cases with less duplication and easier expansion.

## case-insensitive-dictionary
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/case-insensitive-dictionary/item.md`
- Length: `short` (505 words)
- Ideas:
1. When subclassing `dict` for custom key behavior (e.g. case-insensitive keys with `key.casefold()`), also override methods like `.get()`, `.update()`, and `__contains__()` because built-in `dict` methods bypass your `__getitem__`/`__setitem__` overrides.

## case-insensitive-string-class
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/case-insensitive-string-class/item.md`
- Length: `short` (745 words)
- Ideas:
1. When subclassing `str` for custom behavior (like a case-insensitive `CIStr`), wrap inherited string methods with a class decorator that converts `str` arguments to your subclass and converts returned `str`/`list[str]`/`tuple[str]` values back, so chaining methods preserves the custom type.

## chatroom-server-tutorial
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/chatroom-server-tutorial/item.md`
- Length: `long` (4636 words)
- Ideas:
1. Store a callback in `selectors.register(..., data=callback)` and dispatch with `key.data(key.fileobj)` to build a tiny event loop without `if`/`elif` socket-type checks.
2. Use `selectors.DefaultSelector()` to manage many client sockets and only call `recv()` on sockets returned by `select()`, avoiding blocking reads on idle clients.
3. When broadcasting to connected sockets, skip the sender with an identity check (`if client_sock is not sender_sock`) to prevent clients from receiving their own messages back.
4. Pass `0` to `selector.select(0)` for a non-blocking poll of writable sockets before broadcasting, so the server loop can continue immediately.
5. Start a background input thread with `threading.Thread(target=self._input_loop).start()` so a chat client can wait for user input and receive socket messages concurrently.

## connecting-python-and-dyalog-apl-with-sockets
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/connecting-python-and-dyalog-apl-with-sockets/item.md`
- Length: `medium` (1295 words)
- Ideas:
1. Build a minimal local TCP server for quick experiments with `socket.socket(socket.AF_INET, socket.SOCK_STREAM)`, then `bind(("localhost", port))`, `listen()`, and `client, addr = server.accept()`.
2. When using Python sockets, send text as bytes (`b"..."`) or with `.encode()`, and convert received data back with `.decode()` after `client.recv(...)`.
3. Use `client.recv(1024)` to read up to a fixed number of bytes from a socket, and remember it blocks by default until data arrives (just like `accept()` waits for a connection).

## context-managers-with-contextlib-contextmanager
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/context-managers-with-contextlib-contextmanager/item.md`
- Length: `short` (373 words)
- Ideas:
1. Show that a `@contextlib.contextmanager` generator can be used both in a `with` block and as a decorator (same setup/teardown code in one place), using a small timing/logging example with `yield` inside `try/finally`.

## contributing-to-open-source-rich-2073
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/contributing-to-open-source-rich-2073/item.md`
- Length: `long` (5829 words)
- Ideas:
1. When debugging pretty-printing bugs, test the string-returning helper (like `pretty_repr`) first instead of the printing wrapper, so your assertions stay simple and deterministic.
2. If a custom object's `__repr__` can span multiple lines, preserve its alignment when nesting it by indenting continuation lines with the exact prefix width already printed (`key`, `: `, and outer indentation).
3. Use `assert obj is not None` right before attribute access to make your runtime assumption explicit and let MyPy narrow the type in that code path.
4. Add a tiny repro class with a multi-line `__repr__` in tests to reproduce formatting edge cases without depending on heavy third-party libraries like NumPy.
5. For fast local experiments in a library module, put temporary demo code under `if __name__ == "__main__":` and run it with `python -m package.module` to exercise internals quickly.

## counting-passwords-with-automatons
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/counting-passwords-with-automatons/item.md`
- Length: `long` (4903 words)
- Ideas:
1. Count paths in a DAG with recursion + memoization: cache results per state (e.g. tuple keys) so repeated subproblems are computed once.
2. Represent combinatorial progress as a tuple of counters (like `(lower, upper, digits)`) and derive properties such as total length with `sum(state)` instead of storing full strings.
3. Generate tuple-based neighbor states immutably with `enumerate` and slicing, e.g. increment each position once to produce all possible next states.
4. Turn a boolean condition into a numeric contribution with `int(...)`, e.g. start an accumulator with `int(is_terminal(state))` when counting valid endpoints.
5. Make validation rules configurable by composing predicates, e.g. `all(pred(state) for pred in predicates)` to swap password constraints without changing traversal logic.

## creating-programming-language
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/creating-programming-language/item.md`
- Length: `short` (436 words)
- Ideas:
1. Build a tiny Python REPL that preserves variables between commands by reusing the same namespace dictionary in repeated `exec(...)` calls (e.g. `ns = {}; exec(line, ns, ns)`) instead of creating a fresh scope each time.

## custom-json-encoder-and-decoder
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/custom-json-encoder-and-decoder/item.md`
- Length: `medium` (1625 words)
- Ideas:
1. Build extensible JSON encoding by subclassing `json.JSONEncoder` and overriding `default()` to dispatch to methods like `encode_<typename>` via `getattr`, so each supported type gets a small dedicated encoder.
2. When encoding non-standard objects as JSON dicts, add a reserved marker key like `"__extended_json_type__"` with the original type name to help your decoder distinguish custom objects from ordinary dictionaries.
3. Subclass `json.JSONDecoder` and set `object_hook` in `__init__`; in the hook, look up `decode_<type>` from the marker key and fall back to returning the original dict on `KeyError`/`AttributeError` for safe mixed JSON decoding.

## customising-object-creation-with-__new__
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/customising-object-creation-with-__new__/item.md`
- Length: `medium` (1544 words)
- Ideas:
1. Show that `__init__` is only called if `__new__` returns an instance of the class (or a subclass): return a plain `int` from `__new__` and prove `__init__` is skipped.
2. Use `__new__` as a constructor-level factory: make a base class return different subclasses (e.g. `EvenNumber` vs `OddNumber`) based on the input value, `pathlib.Path`-style.
3. When subclassing an immutable type like `float`, override `__new__` to strip extra kwargs before calling `super().__new__`, then store custom attributes (like `rel_tol`) in `__init__`.

## deducing-physics-formulas-with-genetic-algorithms
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/deducing-physics-formulas-with-genetic-algorithms/item.md`
- Length: `long` (3815 words)
- Ideas:
1. Generate random expression trees with a decreasing recursion probability (e.g. pass `operator_prob/2` on recursive calls) to avoid runaway tree growth while still getting varied formulas.
2. Use `random.choices(population, weights=..., k=...)` to bias selection toward fitter candidates without removing randomness (great for parent selection in genetic algorithms).
3. Keep formula evaluation simple by treating invalid operations as a huge penalty: return `float('inf')` on division by zero and let the fitness function handle it.
4. Bind fixed arguments into a reusable sort key with `functools.partial`, e.g. `partial(Formula.fitness, data_list=data)` before calling `sorted(..., key=...)`.
5. Model arithmetic expressions as recursive classes (`Operator` and `Operand`) and implement polymorphic `eval()` so each node knows how to evaluate itself from a data dictionary.

## dipping-my-toes-in-metaprogramming
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/dipping-my-toes-in-metaprogramming/item.md`
- Length: `long` (2791 words)
- Ideas:
1. Use `__new__(cls, ...)` (not `__init__`) when you need construction-time dispatch, e.g. returning `OddNumber` or `EvenNumber` from `Number(value)` based on the input.
2. Guard factory-style `__new__` logic with `if cls is BaseClass:` so subclasses keep their own construction behavior instead of being re-routed by the base class.
3. Call `super().__new__(target_cls)` to create an instance of a specific class; the class you pass to `__new__` determines the actual object type returned.
4. Leverage the rule 'Python only runs `__init__` if `__new__` returns an instance of the requested class' to explain why returning a different object (or a plain value) skips initialization.
5. When exploring unfamiliar dunder methods, add temporary `print()`s inside `__new__` to observe when Python calls it and what gets returned during instantiation.

## divmod-for-unit-conversions
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/divmod-for-unit-conversions/item.md`
- Length: `short` (716 words)
- Ideas:
1. Use `divmod()` repeatedly to break a total into larger units (e.g. `seconds -> minutes/seconds -> hours/minutes`) instead of writing separate `//` and `%` operations at each step.

## dynamic-string-replacements-with-regex
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/dynamic-string-replacements-with-regex/item.md`
- Length: `short` (619 words)
- Ideas:
1. Use `re.sub(..., replacer, text, flags=re.MULTILINE)` with a callback plus a captured `itertools.count(1)` counter to auto-number matching Markdown headings (e.g., turn each `## Title` into `## 01 - Title`) without editing lines manually.

## elo-rating-system-simulation
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/elo-rating-system-simulation/item.md`
- Length: `long` (3397 words)
- Ideas:
1. Split rating logic into a pure `score_delta(expected, actual)` method and a mutating `update_score(...)` method so you can unit-test the math without changing object state.
2. Use `ABC` + `@abstractmethod` to model simulation archetypes (e.g. `Player.generate_score()`), then add new behaviors by subclassing instead of filling the code with `if`/`elif` branches.
3. Decay a parameter gradually with a floor using `self.K = max(FINAL_K, self.K - 1)` after each interaction to reduce volatility over time without letting it drop too far.
4. When two ratings change together, compute one side and derive the other with complements (`1 - expected`, `1 - actual`) to keep updates consistent and avoid duplicate formulas.
5. In long simulation loops, sample metrics periodically with a modulo counter (`c = (c + 1) % interval; if not c: ...`) instead of recording every step.

## finding-and-fixing-over-10000-bugs-on-github
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/finding-and-fixing-over-10000-bugs-on-github/item.md`
- Length: `short` (341 words)
- Ideas:
1. Replace hardcoded alphabet strings (like 'abcdefghijklmnopqrstuvwxy') with `import string` and `string.ascii_lowercase` / `string.ascii_uppercase` to avoid silent typos and make intent explicit.

## finding-similar-photos
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/finding-similar-photos/item.md`
- Length: `long` (2532 words)
- Ideas:
1. Use `PIL.ImageChops.difference()` to compute a pixel-wise diff between two images, then average channel values to get a simple similarity score.
2. When comparing every pair of files, use `itertools.combinations(items, 2)` instead of `product(..., repeat=2)` to avoid duplicate `(A, B)` / `(B, A)` checks.
3. Store pairwise comparison results with an order-independent key like `tuple(sorted((str(f1), str(f2))))` so repeated comparisons are easy to skip.
4. Normalize custom metrics to a `0..1` range (for example, dividing pixel difference by `255`) so thresholds like `0.07` are easier to reason about.
5. Create tiny image summaries with `img.resize((16, 16))` before comparing photos to make near-duplicate detection much faster and less sensitive to detail.

## floodfill-algorithm-in-python
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/floodfill-algorithm-in-python/item.md`
- Length: `long` (8032 words)
- Ideas:
1. Implement grid traversal neighbors with a reusable offsets tuple like `OFFSETS = ((1,0), (-1,0), (0,1), (0,-1))` and loop over it instead of writing four separate checks.
2. In flood fill, mark cells as tracked when you enqueue them (not when you pop them) to prevent duplicate work and repeated processing.
3. Add an early return guard like `if walls[y][x]: return` before flood-filling so clicks/starts on blocked cells are ignored cheaply.
4. Make `floodfill()` return the set of visited cells, then reuse it to count disconnected regions by scanning the grid and flood-filling each unseen open cell.
5. Use a `fringe` set plus `next_fringe` set for layer-by-layer flood fill when you want a wave/spread effect (useful for animations or distance-like expansion).

## functools-cmp_to_key
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/functools-cmp_to_key/item.md`
- Length: `medium` (1190 words)
- Ideas:
1. Use `functools.cmp_to_key` to sort with a legacy two-argument comparator: define `cmp(a, b)` returning negative/zero/positive, then call `sorted(items, key=cmp_to_key(cmp))` instead of rewriting logic immediately.
2. A `key=` function does not need to return numbers; it can return any object Python can compare, so you can build custom sortable wrappers by implementing methods like `__lt__` and `__eq__`.
3. When adapting comparison-based code, wrap each item in a small class (like `K`) that stores the original object and delegates comparisons to your `cmp` function; this lets `sorted()`, `min()`, and `max()` work with old comparison semantics.

## functools-partial
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/functools-partial/item.md`
- Length: `medium` (2317 words)
- Ideas:
1. Use `functools.partial` to freeze non-leading parameters by name (e.g. `partial(func, b=10)`) so you can specialise a function without rewriting it as a `lambda`.
2. Remember that keyword arguments frozen with `partial(...)` are defaults, not locks: callers can still override them when calling the partial function.
3. Inspect a partial object with `.func`, `.args`, and `.keywords` to debug callbacks or understand exactly which arguments were pre-filled.

## functools-placeholder
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/functools-placeholder/item.md`
- Length: `medium` (866 words)
- Ideas:
1. Python 3.14 tip: use `functools.Placeholder` with `partial()` to freeze a later positional-only argument, e.g. `expand_tabs = partial(str.replace, _P, '\t', '    ')` so the string is supplied later.
2. When `partial()` fails with keyword arguments, check `help()` for a `/` in the signature (positional-only params); if the argument you want to freeze is positional-only, use `Placeholder` instead of `name=value`.
3. Make `functools.Placeholder` ergonomic by importing it as `_P` (not `_`) and use it directly as a singleton in `partial(...)` without instantiating it.

## generalising-itertools-pairwise
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/generalising-itertools-pairwise/item.md`
- Length: `medium` (912 words)
- Ideas:
1. Use `itertools.pairwise` to compute consecutive deltas (e.g., balance changes) by looping with `for before, after in pairwise(values):` and subtracting `after - before`.
2. Avoid `zip(data, data[1:])` for `pairwise`-style logic on generators/files; build a rolling window with `deque(maxlen=2)` + `islice` so it works with any iterable.
3. Create a quick `triplewise` by composing `pairwise` twice (`pairwise(pairwise(iterable))`) and unpacking `for (a, _), (b, c) in ...` to yield overlapping triples.

## hold-my-parentheses-light-now-with-zero-sugar
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/hold-my-parentheses-light-now-with-zero-sugar/item.md`
- Length: `short` (445 words)
- Ideas:
1. Use Boolean arithmetic to simplify counter updates in loops (e.g. `depth += (char == '(')` and `depth -= (char == ')')`) because `True`/`False` behave like `1`/`0` in Python.

## hold-my-parentheses
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/hold-my-parentheses/item.md`
- Length: `medium` (857 words)
- Ideas:
1. Use Boolean arithmetic (`True == 1`, `False == 0`) to encode characters as deltas, e.g. `(c == '(') - (c == ')')`, turning a string scan into `+1/0/-1` values.
2. Validate parenthesis order by tracking nesting depth and rejecting as soon as it goes negative (equal counts alone are not enough for strings like `')))((('`).
3. When you need both a running check and a final total from the same sequence, don’t reuse one generator twice; store the values first (e.g. a list of deltas) and then run `accumulate`/`sum` on that list.

## how-to-create-a-python-package-in-2022
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/how-to-create-a-python-package-in-2022/item.md`
- Length: `long` (6796 words)
- Ideas:
1. Use TestPyPI as a safe dry run before real releases: configure Poetry with `poetry config repositories.testpypi https://test.pypi.org/legacy/`, set a TestPyPI API token, then `poetry build` and `poetry publish -r testpypi`.
2. Map GitHub Actions Python versions to tox environments with `tox-gh-actions` so CI doesn't rerun every tox env on every matrix job (add a `[gh-actions]` section in `tox.ini` like `3.10: py310, linting`).
3. Make coverage checks enforceable in CI by adding `coverage report --fail-under 100` to `tox.ini`, so tox fails when coverage drops below your target.
4. Generate `coverage.xml` in a predictable path (for example `coverage xml -o {toxinidir}/coverage.xml`) so Codecov can reliably find and upload coverage results from tox runs.
5. Use a `src/` layout for packages (`src/yourpkg/`) to catch import-path mistakes during testing, and update tool configs (like Scriv version path or tox commands) to point at the new location.

## how-to-work-with-case-insensitive-strings
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/how-to-work-with-case-insensitive-strings/item.md`
- Length: `medium` (2053 words)
- Ideas:
1. When building a case-insensitive container by subclassing `set`/`dict`, override all mutation paths (`update`, `setdefault`, `pop`, etc.) or unsupported methods will bypass your normalization logic.
2. Implement a case-insensitive dictionary that preserves original key casing by storing a mapping from `key.casefold()` to `(original_key, value)` instead of storing only normalized keys.
3. Use a quick Unicode sanity check in the REPL to prove why naive case normalization can break round-trips: compare characters where `c.lower() == c` but `c.upper().lower() != c`.

## huehue
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/huehue/item.md`
- Length: `short` (330 words)
- Ideas:
1. Generate a smooth colour-tile puzzle board by bilinearly interpolating RGB values from the four fixed corner colours (use `zip` + `int()` to lerp each channel across rows and columns).

## ignoring-exceptions-with-contextlib-suppress
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/ignoring-exceptions-with-contextlib-suppress/item.md`
- Length: `short` (664 words)
- Ideas:
1. Wrap your `if __name__ == "__main__":` entrypoint in `with contextlib.suppress(KeyboardInterrupt): main()` to let users cancel with Ctrl+C without showing a traceback.

## itertools-batched
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/itertools-batched/item.md`
- Length: `medium` (1764 words)
- Ideas:
1. Use `map(''.join, batched(text, n))` to turn character batches into fixed-width substrings (great for parsing codons or chunked text) without manual slicing loops.
2. `itertools.batched(...)` is a one-shot iterator, not a reusable container: if you need multiple passes, materialize it first with `pages = list(batched(data, n))`.
3. On Python <3.12, implement a `batched` helper with `itertools.islice` and `while batch := tuple(islice(it, n)):` and validate `n >= 1` by raising `ValueError`.

## julia-set
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/julia-set/item.md`
- Length: `short` (258 words)
- Ideas:
1. Use Python’s built-in `complex` type to prototype a Julia set: iterate `z = z*z + c` in a loop and stop early when `abs(z) > 2` to detect points that escape.

## learn-pandas-and-matplotlib-with-pokemon
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/learn-pandas-and-matplotlib-with-pokemon/item.md`
- Length: `long` (12481 words)
- Ideas:
1. Use `df[stats].sum(axis=1)` to compute a row-wise total column in one shot instead of adding each column manually.
2. Convert several pandas columns at once with `df.astype({...})`, e.g. turning `0/1` flags into `bool` and text labels into `category`.
3. When filtering pandas data, use `&`, `|`, and `~` (with parentheses) instead of Python's `and`, `or`, and `not`.
4. Use `Series.fillna(other_series)` to fill missing values from another column by index alignment (e.g. fill missing secondary type from primary type).
5. For ordered categorical plots, make the column categorical and use `value_counts(sort=False)` so counts follow category order instead of frequency order.

## longest-unique-substring
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/longest-unique-substring/item.md`
- Length: `medium` (1914 words)
- Ideas:
1. When brute-forcing substrings, iterate `stop` backwards and `break` on the first unique match for each `start` to skip checking shorter candidates that can't beat it.
2. Use a sliding window with a string: if `char in window`, do `window = window[window.find(char)+1:]` before appending `char` to keep the window unique with very little code.
3. For a faster unique-character window, pair `collections.deque` with a `set`: append new chars, and on duplicates `popleft()` until the repeated char is removed, giving O(1) membership checks.

## lsbasi-apl-part1
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/lsbasi-apl-part1/item.md`
- Length: `long` (2921 words)
- Ideas:
1. Tokenize right-to-left by starting at `len(code) - 1` and decrementing a cursor, which can simplify parsers for languages/operators that associate from the right.
2. Use a `current_char = None` sentinel when a tokenizer runs out of input so loops like whitespace skipping and digit scanning terminate cleanly without extra bounds checks.
3. Define token groups as class constants (for example `FUNCTIONS` and `MONADIC_OPS`) to make parser conditions clearer than repeating raw token names in multiple places.
4. Implement a recursive-descent parser directly from your grammar with one `parse_<rule>()` method per rule; this keeps the code aligned with the language specification and easier to debug.
5. Add `eat(expected_type)` and `peek()` helper methods to your parser so you can validate the current token and do one-token lookahead without duplicating cursor logic.

## lsbasi-apl-part2
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/lsbasi-apl-part2/item.md`
- Length: `long` (4376 words)
- Ideas:
1. Implement a `NodeVisitor` base class that dispatches with `getattr(self, f'visit_{type(node).__name__}', ...)` to keep AST traversal logic modular and extensible.
2. Tokenize left-to-right and reverse the token list at the end when a right-to-left parser would otherwise make identifier-vs-number parsing awkward (e.g., names like `var1`).
3. Use decorators to make scalar functions automatically recurse over nested Python lists, so one implementation works for both scalars and arrays.
4. Model function application AST nodes as `Monad(function, arg)` / `Dyad(function, left, right)` instead of storing arguments inside function/operator nodes, which simplifies later parser and interpreter extensions.
5. Pass contextual evaluation state (like function valence) through recursive visitor calls with `**kwargs` so the same AST node type can behave differently in monadic vs dyadic contexts.

## lsbasi-apl-part3
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/lsbasi-apl-part3/item.md`
- Length: `long` (7116 words)
- Ideas:
1. Use module-based dispatch for interpreters: map token types to Python functions with `getattr(functions, token.type.lower(), None)` instead of a long `if/elif` chain.
2. When tokenizing line comments, add a `skip_comment()` method that advances until `\n`, and call it alongside whitespace skipping before reading the next token.
3. Normalize equivalent separators in a tokenizer by mapping both a custom separator (like `⋄`) and `\n` to the same token type through a single character-to-token dictionary.
4. Convert a flat index into multidimensional coordinates with repeated `divmod` over reversed radices (an `_encode` helper), which is great for grid/index-generation code.
5. In a recursive-descent parser, add a `peek_beyond_parens()` helper to look past consecutive `)` tokens before deciding whether they belong to a grouped expression or a parenthesized value.

## lsbasi-apl-part4
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/lsbasi-apl-part4/item.md`
- Length: `medium` (1825 words)
- Ideas:
1. In `unittest`, create a custom `TestCase` that decorates `assert*` methods to preprocess inputs (e.g. evaluate DSL/code strings) so tests stay short and readable.
2. When implementing a tokenizer, support decimals like `.3` by treating an empty integer part as `'0'` and allowing `.` as a valid number-start character.
3. Make parser code easier to maintain by grouping repeated token-type checks into a constant (e.g. `ARRAY_TOKENS`) instead of duplicating long lists in multiple conditions.

## mandelbrot
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/mandelbrot/item.md`
- Length: `medium` (880 words)
- Ideas:
1. Use Python’s built-in `complex` type to map screen pixels into the complex plane (e.g. `c = complex(re, im)`) when generating math visualisations like the Mandelbrot set.
2. Implement an escape-time loop with `while it < MAX_ITER and abs(z) < 2:` and use the final iteration count to decide whether a point belongs (or how fast it escapes).
3. Precompute a colour palette once (for example with `pygame.Color().hsla`) and color each pixel with `palette[it]` instead of branching per pixel for faster, cleaner fractal rendering code.

## manipulate-boolean-values-with-conditions
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/manipulate-boolean-values-with-conditions/item.md`
- Length: `short` (649 words)
- Ideas:
1. Replace `if/else` blocks that only return or assign `True`/`False` with the condition itself (e.g. `return len(tup) == 3 and tup[0] <= tup[1] <= tup[2]`) to remove Boolean-manipulating code smells and simplify nested checks via short-circuiting.

## max-is-broken
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/max-is-broken/item.md`
- Length: `short` (488 words)
- Ideas:
1. Use `max(iterable, default=...)` to safely handle empty iterables, e.g. `max(nums, default=float('-inf'))`, and wrap it with `functools.partial` if you want a reusable `pedantic_max` helper.

## minesweeper-remake
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/minesweeper-remake/item.md`
- Length: `short` (158 words)
- Ideas:
1. Model a Minesweeper grid with complex coordinates (e.g., `x + y*1j`) and predefine the 8 neighbour offsets so you can count adjacent mines with `sum((pos + d) in mines for d in OFFSETS)` cleanly and without nested loops.

## minimax-algorithm-and-alpha-beta-pruning
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/minimax-algorithm-and-alpha-beta-pruning/item.md`
- Length: `long` (5159 words)
- Ideas:
1. Toggle turns in recursive game-search code with `not is_maximising` instead of manually branching on the next player.
2. Use `float('-inf')` and `float('+inf')` as initial sentinel scores when implementing running `max`/`min` logic in search algorithms.
3. Collapse duplicated maximise/minimise loops by selecting both the initial value and aggregator with a conditional expression, e.g. `(float('-inf'), max)` vs `(float('+inf'), min)`.
4. Use `max()`/`min()` over a generator expression with `default=` to evaluate child nodes lazily while still handling nodes with no children safely.
5. Implement alpha-beta pruning with an early `break` in the child loop once `beta <= alpha` to skip branches that can no longer affect the final decision.

## module-collections-overview
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/module-collections-overview/item.md`
- Length: `medium` (923 words)
- Ideas:
1. Use `collections.namedtuple` to create lightweight record types that keep tuple behavior (indexing/unpacking) while adding named attributes, e.g. `Point = namedtuple('Point', ['x', 'y'])`.
2. With `collections.deque`, use `appendleft`/`popleft` for fast operations on both ends, and remember `extendleft(iterable)` inserts in reverse order of the iterable.
3. Use `collections.defaultdict(list)` to group values by key without manual key initialization, so you can write `groups[key].append(value)` directly inside a loop.

## module-compression-overview
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/module-compression-overview/item.md`
- Length: `medium` (1666 words)
- Ideas:
1. Python 3.14 tip: import compression algorithms via `from compression import ...` and remember `zstd` is only available as `from compression import zstd` (not `import zstd`).
2. When using `bz2.open`, `gzip.open`, `lzma.open`, or `zstd.open`, explicitly use `"rt"`/`"wt"` for text because compression-module `open()` defaults to binary mode.
3. For incremental compression (e.g. `zstd.ZstdCompressor`), always append the result of `.flush()` after feeding chunks with `.compress()`, because some compressed bytes are buffered until the end.

## module-enum-overview
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/module-enum-overview/item.md`
- Length: `long` (3379 words)
- Ideas:
1. Use `Enum.__members__` to inspect aliases, because iterating an enum skips duplicate-value names and only shows unique members.
2. Add `@unique` to enum classes during definition to fail fast when two members accidentally share the same value.
3. Use `@verify(CONTINUOUS)` on integer enums to catch missing values (gaps) when numbering members manually.
4. Use `@verify(NAMED_FLAGS)` on `Flag` enums so composite aliases (like `WHITE = 7`) only reference defined named flags.
5. When backporting `StrEnum` with `class X(str, Enum)`, avoid `auto()` unless you want integer values, because it won’t auto-generate lowercase strings.

## module-itertools-overview
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/module-itertools-overview/item.md`
- Length: `long` (2913 words)
- Ideas:
1. Use `itertools.chain.from_iterable(...)` to flatten a list (or stream) of iterables lazily instead of building an intermediate list with nested loops.
2. Use `itertools.groupby(...)` to count consecutive runs (like win streaks), and remember it groups adjacent equal keys only, not all matching values globally.
3. Use `itertools.compress(data, selectors)` when you already have a parallel boolean mask and want to filter items without recomputing predicates.
4. Use `itertools.dropwhile(...)` and `itertools.takewhile(...)` for prefix-based filtering on sorted/ordered data (they stop once the first non-matching streak ends).
5. Use `itertools.starmap(func, iterable_of_args)` when your arguments are stored as tuples, so you can call functions without manual unpacking in a loop.

## module-pathlib-overview
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/module-pathlib-overview/item.md`
- Length: `medium` (1239 words)
- Ideas:
1. Use `Path.replace(target)` instead of `Path.rename(target)` when the destination may already exist and you want to overwrite it safely.
2. Build paths with `Path(...) / "subdir" / "file.txt"` (instead of manual string joins) so your code stays portable across Windows and POSIX.
3. Create files/folders idempotently with `Path(...).touch(exist_ok=True)` and `Path(...).mkdir(exist_ok=True)` to avoid `FileExistsError` in repeat runs.

## more-animations-from-first-principles-in-5-minutes
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/more-animations-from-first-principles-in-5-minutes/item.md`
- Length: `medium` (1532 words)
- Ideas:
1. Draw all 3 edges of a triangle in one loop by pairing `points[idx - 1]` with `points[idx]`; when `idx == 0`, negative indexing wraps to the last vertex automatically.
2. When a recursive drawing only needs a size threshold, compare squared distances (e.g. `dx*dx + dy*dy < limit`) instead of computing a square root on every call.
3. Generate an equilateral triangle from a centre point with trig: compute vertices at angles `offset + (0, 2*pi/3, 4*pi/3)` and animate by increasing `length` and `offset` a little each frame.

## mutability-and-random-shuffle
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/mutability-and-random-shuffle/item.md`
- Length: `short` (517 words)
- Ideas:
1. Use `random.sample(seq, k=len(seq))` when you need a shuffled order without mutating the original list (e.g., inside recursion), instead of `random.shuffle(seq)` which shuffles in place.

## n-queens-problem
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/n-queens-problem/item.md`
- Length: `medium` (1181 words)
- Ideas:
1. Use `row + col` and `row - col` invariants to detect diagonal clashes on a chessboard: two positions share a diagonal if either sum or difference matches.
2. Represent one-queen-per-row placements as a tuple of columns (e.g. `(5, 0, 4)`), so `enumerate(placements)` gives `(row, col)` pairs without storing full coordinates.
3. Count backtracking solutions recursively by returning `1` at the base case (`row == n`) and summing recursive calls for each valid next placement.

## parsing-txrx-files-in-python
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/parsing-txrx-files-in-python/item.md`
- Length: `long` (2853 words)
- Ideas:
1. Build a tiny parser by writing a pseudo-BNF grammar first, then implement one Python function per grammar rule (`parse_document`, `parse_node`, etc.) to keep the parser easy to reason about.
2. Make any iterator peekable with a unique sentinel object and a cached `_next` value so you can inspect the next token without consuming it during parsing.
3. Tokenize line-oriented text while preserving line boundaries by yielding a custom newline token (like `"\n"`) after `line.split()`; this makes parsing mixed nested blocks and single-line values much simpler.
4. When converting string tokens to Python types, test float patterns before int patterns (and map `yes`/`no` to booleans) so values are parsed correctly in one pass.
5. Wrap nested parsed dictionaries in a `@dataclass` (e.g., `Node`) and forward `__getitem__` to an internal `values` dict to make deep parser output much easier to query (`node["location"]["reference"]`).

## personal-highlights-of-europython-2025
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/personal-highlights-of-europython-2025/item.md`
- Length: `long` (2974 words)
- Ideas:
1. Show why `0.1 + 0.2 != 0.3`, then teach `math.isclose(...)` as the correct way to compare floats.
2. Use `math.fsum(numbers)` instead of `sum(numbers)` when adding many floats to reduce accumulated rounding error.
3. Use `itertools.groupby` correctly: sort the iterable with the same key function first, then iterate the grouped runs.
4. Use `itertools.compress(data, selectors)` to filter items with a parallel iterable of booleans.
5. Build a custom dict-like type by subclassing `collections.abc.MutableMapping` and implementing the five required core methods.

## personal-highlights-of-pycon-italy-2025
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/personal-highlights-of-pycon-italy-2025/item.md`
- Length: `medium` (2337 words)
- Ideas:
1. Explain the REPL's "appearing built-in": show why `len(dir(builtins))` can increase after evaluating an expression (because `_` gets created), and demonstrate inspecting `builtins._` safely.
2. Show how to add simple localization to a Python CLI with `gettext`: mark user-facing strings with `_()`, load translations, and switch language based on an environment variable or command-line flag.
3. Share a quiz-data validation tip: before running a Python quiz app, validate each question dict so the `correct_answer` is present in `options` (and optionally unique) to prevent broken rounds.

## personal-highlights-of-pycon-us-2025
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/personal-highlights-of-pycon-us-2025/item.md`
- Length: `medium` (1907 words)
- Ideas:
1. Show why `len()` can be misleading for emoji/unicode text, then debug a string by printing each code point with `ord()`/`unicodedata.name()` and count user-visible characters with `regex.findall(r'\X', text)`.
2. Compare `functools.partial` with a `lambda` callback and highlight practical wins: `partial` exposes `.func/.args/.keywords` for introspection and is easier to reuse/debug in event handlers and tests.
3. Use `re.compile(pattern, re.DEBUG)` as a quick regex teaching/debugging trick to inspect how Python parses a complex pattern before running it on real text.

## pigeon-pooping-simulator
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/pigeon-pooping-simulator/item.md`
- Length: `short` (264 words)
- Ideas:
1. Implement an action cooldown in `pygame` using `pygame.time.get_ticks()`: store the last poop timestamp and only allow SPACE to trigger again after a set number of milliseconds.

## pycon-portugal-2025-programme-mafia
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/pycon-portugal-2025-programme-mafia/item.md`
- Length: `medium` (1072 words)
- Ideas:
1. Use `collections.Counter` with `.most_common()` on a list of session speakers to quickly spot who is covering multiple talks/workshops and quantify schedule concentration.
2. Classify “last-minute cancellations” with `datetime.date` arithmetic (for example, `if (event_day - cancelled_on).days <= 5:`) so organisers can trigger emergency replacement workflows automatically.
3. Keep a replacement-speaker waitlist sorted by priority score and use `bisect.insort()` to add/remove candidates efficiently without re-sorting the whole list each time.

## python-deque-tutorial
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/python-deque-tutorial/item.md`
- Length: `long` (3150 words)
- Ideas:
1. Use `collections.deque` for queues: `append()`/`popleft()` stays efficient, unlike `list.pop(0)` which gets slower as the list grows.
2. Create a rolling buffer with `deque(maxlen=n)`: appending new items automatically drops the oldest item from the opposite end.
3. Be careful with `extendleft(iterable)`: it adds items to the left one by one, so the final order is reversed (`deque().extendleft([1,2,3])` -> `3,2,1`).
4. Use `deque.rotate(n)` for circular shifts without manual slicing; positive values move items right, negative values move them left.
5. Use a `deque` as a small internal buffer in iterator-like code: store extra produced items and return them later with `popleft()`.

## python-quiz-europython-2025-edition
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/python-quiz-europython-2025-edition/item.md`
- Length: `medium` (872 words)
- Ideas:
1. Use the `keyword` module to distinguish hard and soft keywords (`keyword.kwlist` vs `keyword.softkwlist`) before generating Python code dynamically.
2. In Python 3.9+, a decorator can be any valid expression (even a `lambda`), so you can write quick one-off decorators like `@lambda f: (lambda x: ...)`—but prefer named decorators for readability.
3. In a fresh REPL, `_` stores the previous result in `builtins` (not `globals()`), which means introspection like `len(dir(builtins))` can change after evaluating an expression.

## python-quiz-pycon-italia-2025-edition
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/python-quiz-pycon-italia-2025-edition/item.md`
- Length: `short` (754 words)
- Ideas:
1. Use `isinstance(True, int)` (and `int(True) == 1`) to remember that `bool` is a subclass of `int` in Python, which matters when summing booleans or validating numeric inputs.

## random-walk-simulations
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/random-walk-simulations/item.md`
- Length: `short` (245 words)
- Ideas:
1. Use modulo arithmetic to make a 2D random walk wrap around screen edges: update `(x, y)` with a step and then do `x %= width; y %= height` so leaving one side reappears on the opposite side.

## recursive-structural-pattern-matching
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/recursive-structural-pattern-matching/item.md`
- Length: `medium` (1677 words)
- Ideas:
1. Use recursive `match` on dataclass-based expression trees (e.g., `Var`, `And`, `Or`, `Not`) to implement an `evaluate(expr, assignments)` function with `all()`/`any()` for nested boolean formulas.
2. Capture dataclass fields directly in pattern cases (like `case Var(name)` or `case And(exprs) | Or(exprs)`) to avoid manual attribute access and make tree-walking code shorter and clearer.
3. Pass state through recursive pattern-matching helpers using optional parameters (e.g., `depth` for indentation and a trailing-comma flag) to build a readable pretty-printer for nested structures.

## regex-in-real-life
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/regex-in-real-life/item.md`
- Length: `short` (794 words)
- Ideas:
1. Parse Markdown headings with one compiled regex using `(?m)` plus named groups (e.g. `^## (?P<nr>\d+) – (?P<title>.*)$`) so you can extract a numeric ID and title directly from file contents without manual line-splitting.

## regex-printer
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/regex-printer/item.md`
- Length: `short` (636 words)
- Ideas:
1. Build a mini regex expander in Python with a recursive-descent parser and an AST visitor: parse `|`, `()`, `[]`, and quantifiers (`*`, `+`, `?`, `{a:b}`), then generate all matches for finite branches and print `...` when repetition would be infinite.

## reverse-engineering-the-chronospatial-computer
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/reverse-engineering-the-chronospatial-computer/item.md`
- Length: `long` (3070 words)
- Ideas:
1. Implement a tiny bytecode interpreter with opcode-indexed dispatch tables (compute, state update, and jump) instead of a long `if/elif` chain.
2. Use a lookup list to decode mixed literal/combo operands (e.g. `[0,1,2,3,A,B,C]`) so operands map to values without conditionals.
3. Turn register assignments into callbacks with `functools.partial(registers.__setitem__, idx)` to keep interpreter steps composable.
4. For base-8 loops that do `A //= 8`, reverse the search by backtracking from the target output and expanding candidates with `next_a*8 .. next_a*8+7`.
5. Optimize VM puzzles by extracting a function that simulates exactly one loop iteration and returns the emitted digit, then use it inside search/backtracking.

## running-python-inside-chatgpt
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/running-python-inside-chatgpt/item.md`
- Length: `long` (6147 words)
- Ideas:
1. Use `sys.version_info` to guard version-specific features (for example, check for Python 3.10+ before importing `itertools.pairwise`).
2. Use `print(..., end=", ")` when you want multiple values on the same line instead of the default newline after each `print`.
3. In the Python REPL, finish a `def` or `class` block with a blank line; continuation lines are shown with `...`, which helps when pasting multiline code.
4. If a recursive solution (like Fibonacci) hits `RecursionError`, rewrite it iteratively with tuple assignment (`a, b = b, a + b`) to handle large inputs safely.
5. With `asyncio.gather(...)`, coroutines run concurrently, so printed results can appear in completion order (shorter sleeps finish first), not the order you passed them in.

## secret-santa-draw-with-python
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/secret-santa-draw-with-python/item.md`
- Length: `medium` (1269 words)
- Ideas:
1. Build a fast exclusion lookup from grouped names with a dictionary comprehension: `exclusions = {name: set(group) for group in groups for name in group}` so `receiver in exclusions[giver]` is quick and readable.
2. Use `for ... else` in retry loops to detect a valid random draw: `break` on an invalid pair, and put the success code in the `else` block so it only runs when no `break` happened.
3. Remember `random.shuffle()` mutates in place; keep a stable reference list and shuffle a copy (`shuffled = names[:]`) so you can safely `zip(original, shuffled)` without losing the original order.

## sets-as-dictionaries-with-no-values
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/sets-as-dictionaries-with-no-values/item.md`
- Length: `long` (2894 words)
- Ideas:
1. Build a tiny set-like container by storing elements as dictionary keys (`self._data[element] = None`), which gives uniqueness and fast membership checks without managing a separate list.
2. When implementing `__iter__` for a custom set-like class, return `iter(self._data)` (or `iter(self._data.keys())`) so the object becomes iterable with one line.
3. Implement an arbitrary `pop()` on a dict-backed container with `key, _ = self._data.popitem(); return key`, and document that callers must not rely on which element is returned.
4. For a custom set-like `__repr__`, special-case the empty container (e.g. `MySet()`) instead of printing `{}`, which is the syntax for an empty dict.
5. Implement methods like `union(*others)` or `intersection(*others)` by copying once and then looping with augmented operators (`|=`, `&=`, `-=`, `^=`) after coercing each input iterable to your set type.

## single-line-calculator
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/single-line-calculator/item.md`
- Length: `medium` (1634 words)
- Ideas:
1. Use the walrus operator to bind a value inside an expression and reuse it immediately, e.g. `(btn := tk.Button(...)) and btn.grid(...)` when you need a one-liner setup.
2. When a module name is only known as a string (or you need an import inside an expression), use `__import__(name)` to load it dynamically, e.g. `tk = __import__("tkinter")`.
3. Build a button grid from a single string with `enumerate()` plus `%` and `//` to compute column/row positions, e.g. loop over `"()C<789/456*123-.0=+"` and place each key from its index.

## sockets-for-dummies
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/sockets-for-dummies/item.md`
- Length: `long` (6519 words)
- Ideas:
1. Use `server.accept()` correctly: keep the listening socket for new connections, and use the returned `client_socket, addr` pair for the actual send/recv conversation.
2. Remember that `socket.recv(n)` returns `bytes`, not `str`: decode with `data.decode('utf8')`, and treat `b''` as a closed connection (EOF from the peer).
3. Build a minimal HTTP response by hand over a socket with a status line, headers, and a blank line before the body (e.g. `b'HTTP/1.1 200 OK\r\nContent-Type: text/html\r\n\r\n<h1>Hello</h1>'`).
4. Implement a simple length-prefixed protocol: send a fixed-width byte header like `b'00012'` for message length, then loop with `recv()` until you collect exactly that many bytes.
5. Don’t assume `socket.send()` transmits everything: either call `sendall()` or write a loop that keeps sending `msg[bytes_sent:]` until the whole payload is sent.

## solving-bridge-repair-in-4ms-with-python
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/solving-bridge-repair-in-4ms-with-python/item.md`
- Length: `medium` (1146 words)
- Ideas:
1. When searching left-to-right expression combinations, try solving backwards: recursively undo the last operand (subtract, divide if divisible, or strip trailing digits) to prune impossible branches early.
2. In recursive backtracking, test the most restrictive operations first (e.g. suffix-match concatenation, then divisibility for multiplication, then addition) so failures happen sooner and the search tree shrinks.
3. Replace broad recursive base cases with a tighter one like `if len(operands) == 1: return target == operands[0]`, and add legality guards (e.g. only try subtraction when `target > last_op`) to cut unnecessary calls.

## solving-mazes-with-programming
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/solving-mazes-with-programming/item.md`
- Length: `short` (562 words)
- Ideas:
1. Treat a 2D grid (or image) as an implicit graph: write a `neighbors(x, y)` generator that yields valid adjacent cells on the fly, so pathfinding algorithms (like Dijkstra/BFS) work without building a full adjacency dictionary first.

## solving-wordle-with-python
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/solving-wordle-with-python/item.md`
- Length: `long` (2859 words)
- Ideas:
1. Use `collections.Counter` as a consumable letter pool when scoring Wordle guesses so repeated letters are only marked `PRESENT` as many times as they actually exist.
2. When handling duplicate letters, build the `Counter` from only non-`CORRECT` positions (e.g. `if s != g`) so exact matches don't get double-counted as misplaced letters.
3. Use a `for ... else` loop in candidate filtering: `break` as soon as a rule fails, and append the word in the `else` block only if every check passed.
4. Turn compact user feedback like `'02010'` into structured values with a mapping dict and a list comprehension, e.g. `sc = [mapping[c] for c in user_input if c in mapping]`.
5. Use the walrus operator to pick and reuse a random value inline, e.g. `print(f"Try {(guess := random.choice(words))!r}.")`.

## teaching-the-worlds-largest-programming-lesson
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/teaching-the-worlds-largest-programming-lesson/item.md`
- Length: `long` (2793 words)
- Ideas:
1. Show the REPL-vs-script difference: in the REPL `age` echoes a value, but in a `.py` file you need `print(age)` to see output.
2. Teach `is None` with a real check like `cell = board.get_vacant_cell(); if cell is None:` to detect a sentinel return value safely.
3. Use a deliberate infinite loop plus an explicit stop condition (`while True:` ... `break`) when you want to keep processing until you find a solution.
4. Highlight zero-based indexing with coordinates (e.g., row `0`, col `2`) so beginners correctly map Python positions to human-readable grid locations.
5. Use an empty-list `for` loop as a natural dead-end handler: if `possible_values` is empty, `for value in possible_values:` runs zero times and your code can skip that branch automatically.

## the-formula-that-plots-itself
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/the-formula-that-plots-itself/item.md`
- Length: `short` (647 words)
- Ideas:
1. Pack a black/white 2D pixel grid into one huge integer by iterating cells in a fixed order, building a bit string of `0/1`, then using `int(bits, 2)` (and multiply by 17 if following Tupper’s formula convention).

## using-an-llm-to-write-a-countdown-timer
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/using-an-llm-to-write-a-countdown-timer/item.md`
- Length: `medium` (2330 words)
- Ideas:
1. Build countdowns from an absolute deadline (`end = time.monotonic() + seconds`) and recompute `remaining = max(0, end - time.monotonic())` each frame instead of subtracting `1 / FPS`.
2. For countdown displays, compute the shown value with ceiling (for example `shown = max(0, math.ceil(end - time.monotonic()))`) so a 3-minute timer starts at `03:00` and doesn’t drift into negative time.
3. When a key should add time to a countdown, update the deadline (for example `end = max(time.monotonic() + 1, end) + 60`) instead of mutating a `remaining` counter, which also makes restarting after zero easy.

## using-uv-to-build-and-install-python-cli-apps
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/using-uv-to-build-and-install-python-cli-apps/item.md`
- Length: `medium` (1873 words)
- Ideas:
1. Use `uv init --app --package myproj` to scaffold a Python project that is immediately ready for a CLI app, then implement your command logic in `src/myproj/__init__.py` inside `main()` so `uv run myproj` executes it.
2. Turn a local Python CLI into a global command with `uv tool install . -e`; the editable install lets code changes in your package take effect immediately without reinstalling after every edit.
3. Customize the command name by editing `[project.scripts]` in `pyproject.toml` (for example, `mywc = "myproj:main"`), then reinstall with `uv tool install . -e --force` because script-entry metadata changes are not picked up automatically.

## uv-cheatsheet
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/uv-cheatsheet/item.md`
- Length: `medium` (981 words)
- Ideas:
1. Use `uv run --with <package> script.py` to try a one-off dependency (like `click`) without adding it to your project or script metadata.
2. Turn a Python script into a runnable standalone tool by adding the shebang `#!/usr/bin/env -S uv run` and making it executable (`chmod +x myscript.py`).
3. Install your own CLI project globally in editable mode with `uv tool install -e .` so command changes are available immediately while you develop.

## water-buckets
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/water-buckets/item.md`
- Length: `medium` (806 words)
- Ideas:
1. Use `math.gcd` (with `functools.reduce`) to reject impossible water-bucket targets early: if `target > max(capacities)` or `target % reduce(gcd, capacities) != 0`, skip the search.
2. Model each bucket configuration as an immutable tuple like `(w1, w2, ...)` so you can store states in a `set` of visited nodes and use them as dictionary keys while searching.
3. Solve bucket puzzles with an implicit-graph BFS (`collections.deque`) that generates next states on the fly (fill, empty, pour) and keeps a `parent` map to reconstruct the shortest sequence of moves.

## what-is-args-and-how-do-you-use-it
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/what-is-args-and-how-do-you-use-it/item.md`
- Length: `short` (582 words)
- Ideas:
1. Show that `*args` is just a naming convention: define `def f(*bananas): ...` to demonstrate that the `*` collects any number of positional arguments into a tuple (including an empty tuple).

## who-wants-to-be-a-millionaire
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/who-wants-to-be-a-millionaire/item.md`
- Length: `short` (218 words)
- Ideas:
1. Use the `keyword` module to check Python soft keywords: `import keyword; print(keyword.softkwlist)` and `keyword.issoftkeyword("match")` let you inspect context-sensitive keywords (like `match`/`case`) without guessing version-specific syntax rules.

## why-apl-is-a-language-worth-knowing
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/why-apl-is-a-language-worth-knowing/item.md`
- Length: `long` (5804 words)
- Ideas:
1. Use boolean arithmetic to remove tiny branches: compute fees with `fee = 200 + 300 * (age <= 24)` instead of an `if`/`else`.
2. Count items matching a condition with `sum(...)`: `count = sum(x == 5 for x in nums)` (or `sum(pred(x) for x in items)`).
3. Treat filtered comprehensions as “select, then transform”: write `[x*x for x in numbers if x % 2 == 0]` by first deciding the filter, then the expression.
4. Sum positive and negative cash flows separately with generator filters: `sum(x for x in flows if x > 0)` and `sum(x for x in flows if x < 0)`.
5. Use `itertools.compress` as a boolean-mask filter (APL-style): `list(compress(numbers, [True, False, True, ...]))` to keep only selected items.

## why-use-enums
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/why-use-enums/item.md`
- Length: `short` (528 words)
- Ideas:
1. Replace string option parameters (e.g. "plain"/"warm"/"casual") with an `Enum` and compare with `is` so callers get IDE autocomplete, avoid typos/casing bugs, and can reuse the same option set across multiple functions.

## writing-interpreter-in-15-loc
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/writing-interpreter-in-15-loc/item.md`
- Length: `long` (2764 words)
- Ideas:
1. Pad `sys.argv` with defaults using `argv.extend([''] * 2)` so you can safely read optional CLI arguments without `IndexError`.
2. Use `io.StringIO` to replace `stdin` when you want a script to accept either interactive input or a pre-supplied input string.
3. Map single-character commands to numeric opcodes with `'+-><,.[]'.find(c)` and branch on integers instead of many string comparisons.
4. Exploit booleans as integers to grow a list on demand, e.g. `t = [0] * (p < 0) + t + [0] * (p == len(t))` for dynamic tape expansion.
5. Track bracket depth with the walrus operator inside a comprehension to find a matching `]`, e.g. updating `d` while scanning the remaining code.

## yamlutils
- Source: `/Users/rodrigogs/Documents/mathspp/mathspp/pages/02.blog/yamlutils/item.md`
- Length: `medium` (884 words)
- Ideas:
1. Parse filenames like `item.en.md` safely with `name, lang, ext = filename.rsplit('.', 2)` so you split from the right and correctly extract the language code.
2. Extract Markdown front matter by splitting on the first two `---` delimiters (header vs body), then load/edit only the YAML header while preserving the post content unchanged.
3. When maintaining translated files, compute the intersection of YAML header key-value pairs across language versions and move only the shared fields into a single `frontmatter.yaml` file.

## Run summary
- Selected: 90
- Succeeded: 90
- Failed: 0
- Retries used: 0

